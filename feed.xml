<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://kate-feingold.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://kate-feingold.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-22T14:29:08+00:00</updated><id>https://kate-feingold.github.io/feed.xml</id><title type="html">blank</title><subtitle>PhD student in Computer Vision at Weizmann Institute of Science, advised by Prof. Tali Dekel. Research interests: generative models, 3D/4D perception, and multimodal learning. </subtitle><entry><title type="html">A Comprehensive Overview of Gaussian Splatting</title><link href="https://kate-feingold.github.io/blog/2023/a-comprehensive-overview-of-gaussian-splatting/" rel="alternate" type="text/html" title="A Comprehensive Overview of Gaussian Splatting"/><published>2023-12-23T17:45:57+00:00</published><updated>2023-12-23T17:45:57+00:00</updated><id>https://kate-feingold.github.io/blog/2023/a-comprehensive-overview-of-gaussian-splatting</id><content type="html" xml:base="https://kate-feingold.github.io/blog/2023/a-comprehensive-overview-of-gaussian-splatting/"><![CDATA[<h4>Everything you need to know about the new trend in the field of 3D representations</h4> <p>Gaussian splatting is a method for representing 3D scenes and rendering novel views introduced in “3D Gaussian Splatting for Real-Time Radiance Field Rendering”¹. <strong>It can be thought of as an alternative to NeRF²-like models</strong>, and just like NeRF back in the day, Gaussian splatting led to <a href="https://github.com/MrNeRF/awesome-3D-gaussian-splatting">lots of new research works</a> that chose to use it as an underlying representation of a 3D world for various use cases. So what’s so special about it and why is it better than NeRF? Or is it, even? Let’s find out!</p> <h3>Table of contents:</h3> <ul><li><a href="#fbb7">TL;DR</a></li><li><a href="#4012">Representing a 3D world</a></li><li><a href="#9bb2">Image formation model &amp; rendering</a></li><li><a href="#6c97">Optimization</a></li><li><a href="#4cd8">View-dependant colors with SH</a></li><li><a href="#3c15">Limitations</a></li><li><a href="#1a92">Where to play with it</a></li></ul> <h3>TL;DR</h3> <p>First and foremost, the main claim to fame of this work was <strong>the high rendering speed</strong> as can be understood from the title. This is due to the representation itself which will be covered below and thanks to the tailored implementation of a rendering algorithm with custom CUDA kernels.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*sJOy-FwxYy918lyscNb21g.png"/><figcaption><strong>Figure 1:</strong> A side-by-side comparison of previous high-quality representations and Gaussian Splatting (marked as “Ours”) in terms of rendering speed (fps), training time (min), and visual quality (Peak signal-to-noise ratio, the higher the better) [Source: taken from [1]]</figcaption></figure> <p>Additionally, Gaussian splatting <strong>doesn’t involve any neural network at all</strong>. There isn’t even a small MLP, nothing “neural”, a scene is essentially just a set of points in space. This in itself is already an attention grabber. It is quite refreshing to see such a method gaining popularity in our AI-obsessed world with research companies chasing models comprised of more and more billions of parameters. Its idea stems from “Surface splatting”³ (2001) so it sets a cool example that classic computer vision approaches can still inspire relevant solutions. Its simple and explicit representation makes Gaussian splatting particularly <strong>interpretable</strong>, a very good reason to choose it over NeRFs for some applications.</p> <h3>Representing a 3D world</h3> <p>As mentioned earlier, in Gaussian splatting a 3D world is represented with a set of 3D points, in fact, millions of them, in a ballpark of 0.5–5 million. Each point is a 3D Gaussian with its own <strong>unique parameters that are fitted per scene</strong> such that renders of this scene match closely to the known dataset images. The optimization and rendering processes will be discussed later so let’s focus for a moment on the necessary parameters.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/631/0*EGVALNS9pxPyXQ8N"/><figcaption><strong>Figure 2:</strong> Centers of Gaussian (means) [Source: taken from Dynamic 3D Gaussians⁴]</figcaption></figure> <p><strong>Each 3D Gaussian is parametrized by:</strong></p> <ul><li>Mean <strong>μ</strong> interpretable as location x, y, z;</li><li>Covariance <strong>Σ</strong>;</li><li>Opacity <strong>σ(𝛼)</strong>, a sigmoid function is applied to map the parameter to the [0, 1] interval;</li><li><strong>Color parameters</strong>, either 3 values for (R, G, B) or spherical harmonics (SH) coefficients.</li></ul> <p>Two groups of parameters here need further discussion, a covariance matrix and SH. There is a separate section dedicated to the latter. As for the covariance, it is chosen to be anisotropic by design, that is, not <a href="https://stats.stackexchange.com/questions/204595/what-is-an-isotropic-spherical-covariance-matrix">isotropic</a>. Practically, it means that <strong>a 3D point can be an ellipsoid rotated and stretched along any direction in space</strong>. It could have required 9 parameters, however, they cannot be optimized directly because a covariance matrix has a physical meaning only if it’s <a href="https://en.wikipedia.org/wiki/Definite_matrix">a positive semi-definite matrix</a>. Using gradient descent for optimization makes it hard to pose such constraints on a matrix directly, that is why it is factorized instead as follows:</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*v5zbNg4P-gpY642L76bAPg.png"/></figure> <p>Such factorization is known as <a href="https://www.visiondummy.com/2014/04/geometric-interpretation-covariance-matrix/#Covariance_matrix_as_a_linear_transformation">eigendecomposition of a covariance matrix</a> and can be understood as a configuration of an ellipsoid where:</p> <ul><li>S is a diagonal scaling matrix with 3 parameters for scale;</li><li>R is a 3x3 rotation matrix analytically expressed with 4 quaternions.</li></ul> <p>The beauty of using Gaussians lies in the two-fold impact of each point. On one hand, each point effectively <strong>represents a limited area</strong> in space close to its mean, according to its covariance. On the other hand, it has a <strong>theoretically infinite extent </strong>meaning that each Gaussian is defined on the whole 3D space and can be evaluated for any point. This is great because during optimization it allows gradients to flow from long distances.⁴</p> <p>The impact of a 3D Gaussian <em>i</em> on an arbitrary 3D point <em>p</em> in 3D is defined as follows:</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/720/1*JGh_0y3ICNuA6IcnbdnvdA.gif"/><figcaption><strong>Figure 3:</strong> An influence of a 3D Gaussian i on a point p in 3D [Source: Image by the author]</figcaption></figure> <p>This equation looks almost like a probability density function of the <a href="https://en.wikipedia.org/wiki/Multivariate_normal_distribution">multivariate normal distribution</a> except the normalization term with a determinant of covariance is ignored and it is weighting by the opacity instead.</p> <h3><strong>Image formation model &amp; r</strong>endering</h3> <h4>Image formation model</h4> <p>Given a set of 3D points, possibly, the most interesting part is to see how it can be used for rendering. You might be previously familiar with a point-wise 𝛼-blending used in NeRF. Turns out that <strong>NeRFs and Gaussian splatting share the same image formation model</strong>. To see this, let’s take a little detour and re-visit the volumetric rendering formula given in NeRF² and many of its follow-up works (1). We will also rewrite it using simple transitions (2):</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*dovqzRKuf4Sf324f-_Smjg.png"/></figure> <p>You can refer to the NeRF paper for the definitions of σ and δ but conceptually this can be read as follows: in NeRF, color in an image pixel <em>p</em> is approximated by integrating over samples (MLP predictions) along the ray going through this pixel. The final color is <strong>a weighted sum of colors of 3D points sampled along this ray, down-weighted by transmittance</strong>. With this in mind, let’s finally look at the image formation model of Gaussian splatting:</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*op2L1Cv4fCMHlYFLnbG0tw.png"/></figure> <p>Indeed, formulas (2) and (3) are almost identical. <strong>The only difference is how 𝛼 is computed</strong> between the two. In Gaussian Splatting, the aggregation for each pixel is conducted over the contribution of an ordered list of projected 2D Gaussians. This small discrepancy turns out extremely significant in practice and results in drastically different rendering speeds. In fact, it is <strong>the foundation of the real-time performance</strong> of Gaussian splatting.</p> <p>To understand why this is the case, we need to understand what <em>f^{2D}</em> means and which computational demands it poses. This function is simply a projection of <em>f(p)</em> we saw in the previous section into 2D, i.e. onto an image plane of the camera that is being rendered. <strong>Both a 3D point and its projection are multivariate Gaussians</strong> so the impact of a projected 2D Gaussian on a pixel can be computed using the same formula as the impact of a 3D Gaussian on other points in 3D (see Figure 3). The only difference is that the mean μ and covariance Σ must be projected into 2D which is done using derivations from EWA splatting⁵.</p> <p>Means in 2D can be trivially obtained by projecting a vector <em>μ </em>in homogeneous coordinates (with extra 1 coordinate) into an image plane using an intrinsic camera matrix <em>K</em> and an extrinsic camera matrix <em>W=</em>[<em>R</em>|<em>t</em>]:</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*3S7fgZeok3k34hoUhaX7xA.png"/></figure> <p>This can be also written in one line as follows:</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*VPkelwwq5tzqhxMvcrOegg.png"/></figure> <p>Here “z” subscript stands for z-normalization. Covariance in 2D is defined using a Jacobian of (4), <em>J:</em></p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*QrPBmBS4_0sOjBB6YgLa1g.png"/></figure> <p>The whole process remains differentiatable, and that is of course crucial for optimization.</p> <h4>Rendering</h4> <p>The formula (3) tells us how to get a color in a single pixel. To render an entire image, it’s <strong>still necessary to traverse through all the HxW pixels</strong>, just like in NeRF, however, the process is much more lightweight because:</p> <ul><li>For a given camera, <em>f(p) </em>of each 3D point <strong>can be projected into 2D in advance</strong>, before iterating over pixels. This way, when a Gaussian is blended for a few nearby pixels, we won’t need to re-project it over and over again.</li><li>There is <strong>no MLP to be inferenced </strong>H·W·P times for a single image, 2D Gaussians are blended onto an image directly.</li><li>There is <strong>no ambiguity in which 3D point to evaluate</strong> along the ray, no need to choose <a href="https://docs.nerf.studio/nerfology/model_components/visualize_samplers.html">a ray sampling strategy</a>. A set of 3D points overlapping the ray of each pixel (see <em>N</em> in (3)) is discrete and fixed after optimization.</li><li>A pre-processing<strong> sorting stage is done once per frame, on a GPU</strong>, using a custom implementation of differentiable CUDA kernels.</li></ul> <p>The conceptual difference can be seen in <strong>Figure 4</strong>:</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/603/1*s6j7Hj9cg9Re9lxlqAHq5w.png"/><figcaption><strong>Figure 4: </strong>A conceptual difference between NeRF and GS, Left: Query a <strong>continuous</strong> MLP along the ray, Right: Blend a discrete set of Gaussians relevant to the given ray [Source: Image by the author]</figcaption></figure> <p>The sorting algorithm mentioned above is one of the contributions of the paper. Its purpose is to prepare for color rendering with the formula (3): sorting of the 3D points by depth (proximity to an image plane) and grouping them by tiles. The first is needed to compute transmittance, and the latter allows to limit the weighted sum for each pixel to α-blending of the relevant 3D points only (or their 2D projections, to be more specific). The grouping is achieved using simple 16x16 pixel tiles and is implemented such that a Gaussian can land in a few tiles if it overlaps more than a single view frustum. Thanks to <strong>sorting, the rendering of each pixel can be reduced to α-blending of pre-ordered points from the tile the pixel belongs to.</strong></p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/581/1*1TfOGhJKCND8JeX6aP-bdg.png"/><figcaption><strong>Figure 5: </strong>View frustums, each corresponding to a 16x16 image tile. Colors have no special meaning. The result of the sorting algorithm is a subset of 3D points within each tile sorted by depth. [Source: Based on the plots from <a href="https://docs.nerf.studio/nerfology/model_components/visualize_samples.html#d-frustum">here</a>]</figcaption></figure> <h3><strong>Optimization</strong></h3> <p>A naive question might come to mind: how is it even possible to get a decent-looking image from a bunch of blobs in space? And well, it is true that if Gaussians aren’t optimized properly, you will get all kinds of pointy artifacts in renders. In Figure 6 you can observe an example of such artifacts, they look quite literally like ellipsoids. The key to getting good renders is 3 components: <strong>good initialization, differentiable optimization, and adaptive densification</strong>.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/877/1*9GwwtPjYiHIBPOR5xa2SBg.png"/><figcaption><strong>Figure 6: </strong>An example of renders of an under-optimized scene [Source: Image by the author]</figcaption></figure> <p>The initialization refers to the parameters of 3D points set at the start of training. For point locations (means), the authors propose to use a point cloud produced by SfM (Structure from Motion), see Figure 7. The logic is that for any 3D reconstruction, be it with GS, NeRF, or something more classic, you must know camera matrices so you would probably run SfM anyway to obtain those. <strong>Since SfM produces a sparse point cloud as a by-product, why not use it for initialization?</strong> So that’s what the paper suggests. When a point cloud is not available for whatever reason, a random initialization can be used instead, under the risk of a potential loss of the final reconstruction quality.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/0*QmfJf4pJH9zfenfA.png"/><figcaption><strong>Figure 7:</strong> A sparse 3D point cloud produced by SfM, means initialization [Source: Taken from <a href="https://speciale.ar/publication/privacypreservingsfm/">here</a>]</figcaption></figure> <p>Covariances are initialized to be isotropic, in other words, <strong>3D points begin as spheres</strong>. The radiuses are set based on mean distances to neighboring points such that the 3D world is nicely covered and has no “holes”.</p> <p>After init, a simple Stochastic Gradient Descent is used to fit everything properly. The scene is optimized for <strong>a loss function that is a combination of L1 and D-SSIM</strong> (structural dissimilarity index measure) between a ground truth view and a current render.</p> <p>However, that’s not it, another crucial part remains and that is adaptive densification. It is launched once in a while during training, say, every 100 SGD steps and its purpose is to address under- and over-reconstruction. It’s important to emphasize that <strong>SGD on its own can only do as much as adjust the existing points</strong>. But it would struggle to find good parameters in areas that lack points altogether or have too many of them. That’s where adaptive densification comes in, <strong>splitting points</strong> with large gradients (Figure 8) and <strong>removing points </strong>that have converged to very low values of α (if a point is that transparent, why keep it?).</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/0*rT4bEjhOZyCUqBuR"/><figcaption><strong>Figure 8: </strong>Adaptive densification. A toy example of fitting a bean shape that we’d like to render with a few points. [Source: Taken from [1]]</figcaption></figure> <h3><strong>View-dependant colors with SH</strong></h3> <p>Spherical harmonics, SH for short, play a significant role in computer graphics and were first proposed as a way to learn a view-dependant color of discrete 3D voxels in Plenoxels⁶. View dependence is a nice-to-have property that <strong>improves the quality of renders since it allows the model to represent non-Lambertian effects</strong>, e.g. specularities of metallic surfaces. However, it is certainly not a must since it’s possible to make a simplification, choose to represent color with 3 RGB values, and still use Gaussian splatting like it was done in [4]. That is why we are reviewing this representation detail separately after the whole method is laid out.</p> <p>SH are special functions defined on the surface of a sphere. In other words, you can evaluate such a function for any point on the sphere and get a value. <strong>All of these functions are derived from this single formula</strong> by choosing positive integers for <em>ℓ </em>and −<em>ℓ</em> ≤ <em>m</em> ≤ <em>ℓ</em>, one <em>(ℓ, m)</em> pair per SH:</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/950/0*5KqRQO0HulZp2DQu"/></figure> <p>While a bit intimidating at first, for small values of <em>l</em> this formula simplifies significantly. In fact, for <em>ℓ = 1, Y = ~0.282</em>, just a constant on the whole sphere. On the contrary, higher values of <em>ℓ</em> produce more complex surfaces. The theory tells us that spherical harmonics form an orthonormal basis so<strong> each function defined on a sphere can be expressed through SH</strong>.</p> <p>That’s why the idea to express view-dependant color goes like this: let’s limit ourselves to a certain degree of freedom <em>ℓ_max</em> and say that each <strong>color (red, green, and blue) is a linear combination of the first <em>ℓ_max</em> SH functions</strong>. For every 3D Gaussian, we want to learn the correct coefficients so that when we look at this 3D point from a certain direction it will convey a color the closest to the ground truth one. The whole process of obtaining a view-dependant color can be seen in Figure 9.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/872/1*bKNS_UyAOGcQvew-b-pciQ.png"/><figcaption><strong>Figure 9: </strong>A process of obtaining a view-dependant color (red component) of a point with <em>ℓ_max = 2 and 9 learned coefficients</em>. A sigmoid function maps the value into the [0, 1] interval. Oftentimes, clipping is used instead [Source: Image by the author]</figcaption></figure> <h3>Limitations</h3> <p>Despite the overall great results and the impressive rendering speed, the simplicity of the representation comes with a price. The most significant consideration is various <strong>regularization heuristics</strong> that are introduced during optimization to guard the model <strong>against “broken” Gaussians</strong>: points that are too big, too long, redundant, etc. This part is crucial and the mentioned issues can be further amplified in tasks beyond novel view rendering.</p> <p>The choice to step aside from a continuous representation in favor of a discrete one means that <strong>the inductive bias of MLPs is lost</strong>. In NeRFs, an MLP performs an implicit interpolation and smoothes out possible inconsistencies between given views, while 3D Gaussians are more sensitive, leading back to the problem described above.</p> <p>Furthermore, Gaussian splatting is not free from some well-known <strong>artifacts present in NeRFs</strong> which they both inherit from the shared image formation model: lower quality in less seen or unseen regions, floaters close to an image plane, etc.</p> <p>The file size of a checkpoint is another property to take into account, even though novel view rendering is far from being deployed to edge devices. Considering the ballpark number of 3D points and the MLP architectures of popular NeRFs, both take <strong>the same order of magnitude of disk space</strong>, with GS being just a few times heavier on average.</p> <h3>Where to play with it</h3> <p>No blog post can do justice to a method as well as just running it and seeing the results for yourself. Here is where you can play around:</p> <ul><li><a href="https://github.com/graphdeco-inria/gaussian-splatting">gaussian-splatting</a> — the official implementation with custom CUDA kernels;</li><li><a href="https://github.com/nerfstudio-project/nerfstudio">nerfstudio</a> —yes, Gaussian splatting in <strong>nerf</strong>studio. This is a framework originally dedicated to NeRF-like models but since December, ‘23, it also supports GS;</li><li><a href="https://github.com/DSaurus/threestudio-3dgs">threestudio-3dgs</a> — an extension for threestudio, another cross-model framework. You should use this one if you are interested in generating 3D models from a prompt rather than learning an existing set of images;</li><li><a href="https://github.com/aras-p/UnityGaussianSplatting">UnityGaussianSplatting</a> — if Unity is your thing, you can port a trained model into this plugin for visualization;</li><li><a href="https://github.com/nerfstudio-project/gsplat">gsplat</a> — a library for CUDA-accelerated rasterization of Gaussians that branched out of nerfstudio. It can be used for independent torch-based projects as a differentiatable module for splatting.</li></ul> <p>Have fun!</p> <h3>Acknowledgments</h3> <p>This blog post is based on a group meeting in the lab of Dr. Tali Dekel. Special thanks go to <a href="https://medium.com/u/74c8897cdd08">Michal Geyer</a> for the discussions of the paper, to the authors of [4] for a coherent summary of Gaussian splatting, and to <a href="https://medium.com/u/0170f53deb62">Yuliang Guo</a> for the suggested improvements.</p> <h3>References</h3> <ol><li>Kerbl, B., Kopanas, G., Leimkühler, T., &amp; Drettakis, G. (2023). <a href="https://arxiv.org/abs/2308.04079">3D Gaussian Splatting for Real-Time Radiance Field Rendering.</a> SIGGRAPH 2023.</li><li>Mildenhall, B., Srinivasan, P. P., Tancik, M., Barron, J. T., Ramamoorthi, R., &amp; Ng, R. (2020). <a href="https://arxiv.org/abs/2003.08934">NeRF: Representing Scenes as Neural Radiance Fields for View Synthesis.</a> ECCV 2020.</li><li>Zwicker, M., Pfister, H., van Baar, J., &amp; Gross, M. (2001). <a href="https://www.cs.umd.edu/~zwicker/publications/SurfaceSplatting-SIG01.pdf">Surface Splatting.</a> SIGGRAPH 2001</li><li>Luiten, J., Kopanas, G., Leibe, B., &amp; Ramanan, D. (2023). <a href="https://arxiv.org/abs/2308.09713">Dynamic 3D Gaussians: Tracking by Persistent Dynamic View Synthesis.</a> International Conference on 3D Vision.</li><li>Zwicker, M., Pfister, H., van Baar, J., &amp; Gross, M. (2001). <a href="https://www.cs.umd.edu/~zwicker/publications/EWAVolumeSplatting-VIS01.pdf">EWA Volume Splatting.</a> IEEE Visualization 2001.</li><li>Yu, A., Fridovich-Keil, S., Tancik, M., Chen, Q., Recht, B., &amp; Kanazawa, A. (2023). <a href="https://arxiv.org/abs/2112.05131">Plenoxels: Radiance Fields without Neural Networks.</a> CVPR 2022.</li></ol> <p><img src="https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=e7d570081362" width="1" height="1" alt=""/>&lt;hr&gt;&lt;p&gt;<a href="https://medium.com/data-science/a-comprehensive-overview-of-gaussian-splatting-e7d570081362">A Comprehensive Overview of Gaussian Splatting</a> was originally published in <a href="https://medium.com/data-science">TDS Archive</a> on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</p>]]></content><author><name></name></author><category term="external-posts"/><category term="medium"/></entry><entry><title type="html">Vision Transformer (ViT) under the magnifying glass, Part 1</title><link href="https://kate-feingold.github.io/blog/2023/vision-transformer-vit-under-the-magnifying-glass-part-1/" rel="alternate" type="text/html" title="Vision Transformer (ViT) under the magnifying glass, Part 1"/><published>2023-01-17T06:34:44+00:00</published><updated>2023-01-17T06:34:44+00:00</updated><id>https://kate-feingold.github.io/blog/2023/vision-transformer-vit-under-the-magnifying-glass-part-1</id><content type="html" xml:base="https://kate-feingold.github.io/blog/2023/vision-transformer-vit-under-the-magnifying-glass-part-1/"><![CDATA[<h4>Embeddings</h4> <h3><strong>Introduction</strong></h3> <p>Transformers in NLP and Vision Transformers (ViTs) for Computer Vision have been around for a while. First, “Attention Is All You Need”<a href="#73bd">¹ </a>revolutionized the field of NLP, and then <strong>“An Image is Worth 16x16 Words: Transformers for Image Recognition at Scale”</strong><a href="#3fef">²</a> had a tremendous impact on Computer Vision. The latter is the paper that we’ll dissect in this article. Both works weren’t the first to attempt to use attention in their fields, but they were the first to propose simple and effective architectures. Since then there have been a lot of interesting uses of attention and self-attention layers in Computer Vision. Here are a few:</p> <ul><li>“End-to-End Object Detection with Transformers”<a href="#4efe">³</a>, was released a bit earlier than the ViT we are reviewing <strong>(object detection)</strong>;</li><li>“Swin Transformer: Hierarchical Vision Transformer using Shifted Windows”<a href="#cf8f">⁴</a> <strong>(classification)</strong>;</li><li>“BEIT: BERT Pre-Training of Image Transformers”<a href="#bfd2">⁵</a> <strong>(classification)</strong>;</li><li>“Emerging Properties in Self-Supervised Vision Transformers”<a href="#9b72">⁶</a> <strong>(self-supervised pre-training)</strong>;</li><li>“LoFTR: Detector-Free Local Feature Matching with Transformers”<a href="#8ed3">⁷</a> <strong>(image feature matching)</strong>;</li><li>“Injecting Semantic Concepts into End-to-End Image Captioning”<a href="#4aa8">⁸</a> <strong>(image captioning)</strong>;</li><li>“PCT: Point Cloud Transformer”<a href="#8a47">⁹</a> <strong>(3D point cloud segmentation)</strong>;</li><li>“TransGAN: Two Pure Transformers Can Make One Strong GAN, and That Can Scale Up”<a href="#9022">¹⁰</a> <strong>(image generation)</strong>;</li><li>“StyTr2: Image Style Transfer with Transformers”<a href="#3625">¹¹</a> <strong>(style transfer)</strong>;</li><li>“Zero-Shot Text-to-Image Generation”<a href="#13df">¹²</a> <strong>(text-to-image)</strong>;</li><li>Etc.</li></ul> <p>It is definitely very exciting to witness a certain paradigm shift and to see alternative or at least complementary layers to the plain old convolution. This is possibly one of the reasons why vision transformers have been widely talked about, along with their success in various tasks of computer vision. So most likely you’ve already heard about them and might even know how they work at some level. But if you still feel like there are a few pieces of the puzzle missing, then this series of articles is for you. <strong>They are meant to glue the important details together in an interactive walkthrough over the classic ViT</strong> and answer some engineering and theoretical questions which can arise when seeing this architecture for the first time. We are going to look critically at all layers and explore their properties together with the motivation behind them, according to the authors of the paper and based on other works.</p> <p>Luckily, many visual transformers from recent publications aim to improve ViT by introducing only incremental changes, at least this holds true at the time of publishing this article. It means that understanding how it is built will help you understand other research in the domain better. In fact, some works will be briefly discussed here for their contribution to mitigating specific issues present in the model.</p> <p>Here is a sneak peek into questions that will be addressed in this article:</p> <ul><li>What <strong>layers</strong> are there in the network and how to implement them?</li><li>How many <strong>parameters</strong> does each layer introduce?</li><li>How are the <strong>weights shared</strong> over the input sequence?</li><li>How is the <strong>transition from a convolutional</strong> to an attention layer made?</li><li>Where are the <strong>activations</strong> and what type are they?</li><li>What are the motivation, <strong>strengths, and weaknesses</strong> behind various design choices?</li><li>For example, what is a <strong>LayerNorm</strong> and why not use BatchNorm?</li><li>What is the <strong>[CLS] token</strong> and did anyone try anything else instead for the same purpose?</li><li>What are <strong>positional masks </strong>and<strong> head masks</strong> and when are they used?</li><li>How to do inference in a different <strong>image resolution</strong>?</li><li>How is the whole network <strong>trained</strong> and are there nuances compared to training a CNN?</li><li>Etc.</li></ul> <p>All of these will be answered while moving through the layers one by one.</p> <p><strong>The pre-requisite</strong> for the material presented below is being familiar with the general concepts of building and training a neural network. Prior knowledge in vision will be very helpful, but prior knowledge in NLP is not required. Regardless, it can be useful to anyone who wants to understand any kind of transformer better.</p> <h3>The architecture</h3> <p>The task that the ViT from “An Image is Worth 16x16 Words: Transformers for Image Recognition at Scale”<a href="#3fef">²</a> solves is <strong>image classification</strong>. As with any classification network, it can also serve as a backbone for other tasks and can be even combined with CNN. Below you can see the layers the ViT is composed of. They are listed with references to the sections in the article series discussing them. We’ll take a look at the code for each layer, but please note that <a href="https://github.com/google-research/vision_transformer">the original implementation</a> is written in JAX, while this explanation is based on the implementation of the Hugging Face <a href="https://huggingface.co/google/vit-base-patch16-224">model <em>“google/vit-base-patch16–224”</em></a> in PyTorch and is verified with the paper and with the source code.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/184/0*4uIjZlqXSjbuV_zO"/><figcaption>Figure 1: Model overview, taken from “An Image is Worth 16x16 Words: Transformers for Image Recognition at Scale”<a href="#73bd">¹</a></figcaption></figure> <pre>1 Embedding (Stem)<br />  1.1 Patch Embeddings<br />  1.2 (Optional) Positional masking<br />  1.3 [CLS] token<br />  1.4 Positional embedding<br />  1.5 DropOut<br />2 Encoder<br />  VitLayer x N<br />    2.1 Attention<br />      2.1.1 Layer Normalization <br />      2.1.2 MHSA<br />      DropOut<br />      2.1.3 (Optional) Head masking <br />      2.1.4 Linear<br />      Dropout<br />    2.2 MLP<br />      Layer Normalization<br />      Linear<br />      GeLU<br />      Linear<br />      DropOut<br />  Layer Normalization<br />3 Classifier</pre> <p>The authors chose this architecture deliberately to prove a very powerful yet simple idea: <strong>the great success of Transformers</strong><a href="#73bd"><strong>¹</strong></a><strong> in NLP can be brought to Vision almost out of the box</strong>. What it means in practice is that ViT looks like the encoder part of the Transformer, but does have a few differences. The most obvious one is that the input is an image, so it has to be somehow represented as a sequence. Apart from that, unlike in the Transformer, positional embedding is learnable and normalization precedes each block. We will, of course, get back to these discrepancies later.</p> <p>It can be said with certainty that they succeeded in <strong>bringing NLP and Vision closer together</strong>. For example, we can see relatively recent works like “BEiT: BERT Pre-Training of Image Transformers”<a href="#bfd2">⁵</a> and similar ones, which aim to mimic the masked language modeling task with masked image modeling. More on this when we get to the masking stage. On the other hand, some progress is being made by applying convolutional prior knowledge to Vision Transformers, which we’ll also discuss later. One direction where both fields meet is making transformers faster and more efficient to enable real-time applications. This is relevant both for Computer Vision and NLP, that’s why optimization ideas are often shared. A few examples of works that aim to speed up Vision Transformers are ResT<a href="#a457">¹³</a>, EfficientFormer<a href="#087d">¹⁴</a>, EdgeViT<a href="#5262">¹⁵</a>, etc.</p> <p>In general, there are probably hundreds of ViT-related papers by now. To be able to understand those, it is essential to have a good understanding of the base, so let’s jump into unraveling the architecture. Based on the main stages in it, this article will be split into 3 parts:</p> <ol><li><strong>Part 1:</strong> Embeddings (you are here)</li><li><strong>Part 2:</strong> Encoder (in progress)</li><li><strong>Part 3:</strong> A classification head, model sizes, training, inference, etc (in progress)</li></ol> <p>Additionally, there is also a <a href="https://colab.research.google.com/github/yurkovak/ViT_notebook/blob/main/Vision_Transormer_(ViT)_under_the_magnifying_glass.ipynb"><strong>Colab Notebook</strong></a><strong> </strong>where you can experiment with all the code you’ll find below.</p> <h3>Table of Contents</h3> <ol><li><a href="#4996">Embedding (Stem)</a><br/><a href="#1854">1.1 Patch Embeddings</a><br/><a href="#a295">1.2 (Optional) Positional masking</a><br/><a href="#60c1">1.3 [CLS] token</a><br/><a href="#3b86">1.4 Positional embedding</a><br/><a href="#0b28">1.5 Dropout</a></li></ol> <p><a href="#8077">Summary</a><br/><a href="#c80c">Acknowledgments<br/></a><a href="#f260">References</a></p> <h3>1. Embeddings (Stem)</h3> <p>The embedding is the stage that is there to prepare an input image for the Transformer encoder, that is, the rest of the network. To be a bit more specific, after this stage we’ll have a certain representation, and it will be <strong>impossible to tell just by looking at it whether it came from an image or from a sentence</strong>. In other words, it will have the same shape an embedded text sentence would have.</p> <h3>1.1 Patch embeddings</h3> <p>The embedding of an image starts with splitting it into patches. A bit later, <strong>each patch will be treated as a token in a sentence</strong>. This is where the title is coming from: “an image is worth 16x16 words”, where the notations “word” and “token” are used interchangeably and 16x16 refers to pixels. In NLP a token stands for a unique vector that represents a meaningful word or part of a word, and all tokens together define a vocabulary. The vocabulary of a pre-trained model, e.g. of BERT<a href="#cd8b">¹⁶</a>, is fixed. It is there to map each input token the model gets to its embedding. If you want to learn more about tokenization in NLP you can start for example <a href="https://huggingface.co/course/chapter2/4?fw=pt">here</a>.</p> <p>But if you feel like you don’t fully understand what this all means, don’t worry. You don’t have to, because in ViTs the definition of a token/word is a bit different. In particular, we won’t store any vocabulary of image patches, as it would be redundant. Unlike a linguistic token which is initially represented as a number in a vocabulary, in some sense, <strong>an image patch is already a certain meaningful embedding consisting of pixel values.</strong> So instead of a fixed mapping, a linear layer is applied to each image patch directly, resulting in a unique embedding. Here’s how it’s done.</p> <p>To begin with, a patch size must be defined. It’s a hyperparameter that is set manually by the authors through experimentation and <strong>it cannot be changed</strong> after a model is trained. If you ever see ViTs in various comparison tables, the <em>“/P”</em> suffix in a model name refers to the used patch size, e.g. ViT-L/16 stands for a large model with 16x16 patches. Model sizes introduced in the paper will be discussed in part 3 of the article series.</p> <p>In the ViT, patches do not overlap in any way, you can think of it as a grid. They will “interact” only later, in other layers. But for now, we take each one and apply the same linear layer to it, that is, with shared weights. To apply a linear layer to a PxPx3 image patch we can flatten it into P*P*3 values and use <em>Linear(P*P*3, D)</em>, <em>D</em> for a new dimension. If we look at it again, we can see that <strong>this operation is in fact equivalent to convolving an input image</strong> with stride 16 and <em>D</em> kernels of size 16x16. This eliminates the need to do any flattening and reshaping (for now). Indeed, each patch pixel value in its spatial location and channel (red, green, and blue) gets a unique weight, and the same weight will be applied to a value in the same coordinate in a new patch. Let’s stop right there and take a look at the animation of the process. After all, one animation is worth 1000 words:)</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*CcjZmfOrzU6mDlTljvrqHw.gif"/><figcaption><em>Figure 2: Linear embedding of an RGB input image. [</em>Source: Image by the author.<em>]</em></figcaption></figure> <p>ViT is trained on images in resolution 224x224 on ImageNet<a href="#03dc">²⁴</a> or JFT-300M<a href="#de61">²³</a>, so after the convolution (or a linear layer) this is equivalent to 14x14 patches. Each patch initially has <em>16 * 16 * 3 = 768</em> values, and the convolution has 768 kernels too, namely <em>D = 768</em>. In other words, 768 values of each patch are mapped into 768 new values. This is a pure coincidence, or maybe it would be more correct to call it a design choice. The new dimension D is called <strong>a hidden size</strong> or <strong>latent vector size</strong>, and of course, it is not a must to make it equal to the number of input values in a patch. In fact, <em>D = 768 </em>is also used for models with other patch sizes, like for example in the Hugging Face model <em>“google/vit-base-patch32–384”</em>. In the paper, the authors vary the latent vector size D based on the model size, but keep it the same throughout all layers of one model, except in MLP (you’ll see it in part 2 of the articles).</p> <p>Now that we have 14x14 patches with 768 features in each, the next step is to form a sequence from them. This is done by simply flattening 14x14 elements into 14*14 elements, row after row.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*s4gQTn494tgmkczqlQP7aw.gif"/><figcaption><em>Figure 3: Embeddings flattening [</em>Source: Image by the author.<em>]</em></figcaption></figure> <p><strong>Code:</strong></p> <p>For simplicity, here and in all the snippets below we’ll omit a definition of <em>torch.nn.Module</em> with <em>forward(…)</em> and other methods. Instead, we’ll focus only on required operations, torch layers, and shapes. Obviously, to be able to do inference or train a model, it has to be properly organized into sub-modules, so if you want to see the whole model’s class together you can look at the code in Hugging Face. We’ll import the entire model later on in this article.</p> <p>Back to embeddings:</p> <pre>im_h, im_w = 224, 224<br />num_channels = 3<br />input_image = torch.ones((1, num_channels, im_h, im_w))  # torch.Size([1, 3, 224, 224])<br />patch_size = 16<br />hidden_size = 768<br />projection = torch.nn.Conv2d(num_channels, hidden_size,<br />                             kernel_size=patch_size, stride=patch_size)<br />x = projection(input_image)  # torch.Size([1, 768, 14, 14])<br />embeddings = x.flatten(2).transpose(1, 2)  # torch.Size([1, 196, 768]), 196 = 14*14<br /><br /># Run yoursef here: https://colab.research.google.com/github/yurkovak/ViT_notebook/blob/main/Vision_Transormer_(ViT)_under_the_magnifying_glass.ipynb</pre> <p>Let’s also write some code to prove that a convolutional and a linear layer applied to flattened patches do the same thing. For this, we’ll take weights of Conv2d, reshape them and copy them into weights of Linear, reshape the image ourselves prior to applying the layer and compare results. The results should match.</p> <pre>embeddings_conv = embeddings<br /><br /># Copy weights from Conv2d into Linear<br />conv_weight = projection.weight  # torch.Size([768, 3, 16, 16])<br />conv_bias = projection.bias  # torch.Size([768])<br />linear_projection = torch.nn.Linear(patch_size * patch_size * num_channels, hidden_size)<br />linear_projection.weight = torch.nn.Parameter(copy.deepcopy(conv_weight).flatten(1))  # torch.Size([768, 768])<br />linear_projection.bias = torch.nn.Parameter(copy.deepcopy(conv_bias))  # torch.Size([768])<br /><br /># Patchify the image and project with Linear<br />patched_image = torch.nn.Unfold(kernel_size=patch_size, stride=patch_size)(input_image)  # torch.Size([1, 768, 196])<br />patched_sequence = patched_image.transpose(1, 2)  # torch.Size([1, 196, 768])<br />embeddings_linear = linear_projection(patched_sequence)<br /><br /># Compare the results of Linear and Conv2d<br />print(&#39;Outputs of Linear and Conv2d match:&#39;, <br />      torch.allclose(embeddings_conv, embeddings_linear, atol=1e-03))  # prints True<br /><br /># Run yoursef here: https://colab.research.google.com/github/yurkovak/ViT_notebook/blob/main/Vision_Transormer_(ViT)_under_the_magnifying_glass.ipynb</pre> <p><strong>Parameter count:</strong></p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*HqFegOE4sSuSj9ChGYPw1g.jpeg"/></figure> <p><strong>Let’s dig deeper:</strong></p> <p><strong><em>Weaknesses of the selected patching approach</em></strong></p> <p>As you can see, the separation into patches is very simple and even naive to some extent. To embed a 2D patch, a plain linear layer is used on its flattened representation. Let’s look back in time for a moment and recall that years ago linear layers were commonly used on flattened images to train NNs for tasks like classification. Back then there was a known issue that any two vertically neighboring pixels at coordinates <em>(i, j)</em> and <em>(i+1, j)</em> end up far apart in a flattened representation. Luckily, CNNs with very small kernels addressed this problem by introducing <strong>the local connectivity pattern</strong>. For example, when a small 3x3 kernel with stride 1 is applied to an image, its weights are equally applied to each pixel together with 8 neighboring pixels around it.</p> <p>Well, in the ViT, chunks of an image (patches) much bigger than 3x3 are grouped together and there is no overlap between them. So the same issue from the past, which we can say was solved by CNNs, is in fact present in ViTs <strong>within each patch</strong>. To re-iterate, coordinates <em>(i, j)</em> and <em>(i+1, j)</em> within each patch do end up far apart in flattened vectors. This creates two distinct issues:</p> <ul><li><strong>Due to 16x16 kernels</strong>, or any other big ones, local pixel-level features and relationships are disregarded;</li><li><strong>Due to stride 16</strong>, or any other stride equal to a kernel size, information along patch borders is somewhat underrepresented.</li></ul> <p>To understand the second issue, think about the following example. Imagine you have an image of a puppy. Since patches will “pay attention” to each other later in the attention layers, <strong>it would be probably preferable to have meaningful parts of an image within each patch</strong>. Instead, with a non-overlapping grid of the pre-defined size that we have in the ViT, we can end up with the grid cutting right through the puppy’s face:( Without conducting proper experiments, it’s hard to say how much of a problem patch borders create in practice, but it’s clearly not ideal.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/563/0*zpD2MxerkfEQwexA"/><figcaption><em>Figure 4: You can see how the eyes and the nose ended up split into separate patches, even though they can be considered individual semantic units [</em>Source: Image by the author.<em>]</em></figcaption></figure> <p>The natural solution that comes to mind is to use much <strong>smaller patches</strong> instead. Then we’ll have a smaller kernel, better local connectivity within each patch, and arguably more meaningful and semantically homogeneous chunks of an image. The authors of the paper experimented with this and report that decreasing the patch size shows surprisingly robust improvements. However, it comes with a price and turns out to be a bit trickier than it sounds. First and foremost, the usage of smaller patches increases a sequence length and makes the network computationally more expensive. When you finish part 2 of the article series, you’ll understand exactly why it’s the case. But even if we could afford more computations, Namuk Park et al in their work “How Do Vision Transformers Work?”<a href="http://82df">¹⁷</a> show that extremely small patch sizes, like 2x2, hurt the predictive performance of ViT. It’s fair to speculate that other real-world datasets and image resolutions might also require careful reselection of this parameter.</p> <p><strong><em>Related works</em></strong></p> <p>To deal with these problems some research works explore <strong>more elaborate image-dividing methods</strong>. We won’t dive into the accuracy improvements each one brings and at which computational cost. Instead, we’ll just go over the methods proposed in a few prominent papers, and if you are curious you can of course check their result tables yourself. On the other hand, if you don’t feel like reading about other visual transformers yet, feel free to skip to the next section.</p> <p>A model called <strong>TNT from “Transformer in Transformer”</strong><a href="#1040">¹⁸</a> also uses 16x16 patches, but inside of each patch, there are 4x4 sub-patches. To differentiate, they came up with the following names: they call big patches “sentences” and small patches inside of them “words”. The network has two parallel flows: outer and inner. The outer Transformer is pretty much the same ViT we are reviewing in this article (with minor differences) that operates on sentences. The inner Transformer is actually the same ViT too (minus the [CLS] token you’ll learn about soon), but it operates on 4x4 sub-patches of each sentence independently. The way weights are shared between sentences by the inner Transformer is as if sentences from one image make a batch. Flows get combined together with addition performed in each and every encoder block, that’s why we say that inner and outer flows are parallel. To be a bit more specific, they add 1. sentence-level embeddings and 2. flattened embeddings of all words within a sentence. Here is the <a href="https://gitee.com/mindspore/models/blob/master/research/cv/TNT/src/models/tnt/tnt.py">source code</a> in the MindSpore framework and a <a href="https://github.com/rwightman/pytorch-image-models/blob/main/timm/models/tnt.py">PyTorch implementation</a> by timm.</p> <p>Then, there is also a model called<strong> Swin from “Swin Transformer: Hierarchical Vision Transformer using Shifted Windows”</strong><a href="#cf8f">⁴</a> that actually received the best paper award in ICCV 2021. Forget about two flows of the TNT, Swin has only one. But just like in TNT, it also deals with sub-patches. Instead of the names “sentences” and “words” used in TNT, they call big 28x28 pixels patches “windows” and 4x4 sub-patches inside of them, well, simply “patches”. Since there is no outer flow, Swin applies attention only within each window. And just like in TNT, weights are shared between windows as if they form a batch. But there are two more tricks to know here. One trick is that once in a few blocks, they take groups of 2x2 spatially neighboring hidden vectors and combine them into one with <a href="https://www.tensorflow.org/api_docs/python/tf/nn/space_to_depth">a space-to-depth operation</a>. This way they gradually reduce sequence length so that at the end of the network there is only one window left. The other trick is the shifting mentioned in the title which is done before every other block and reverted back after the block. It’s performed by <a href="https://pytorch.org/docs/stable/generated/torch.roll.html"><em>torch.roll</em></a> and the analogy for it is a treadmill: whatever disappears from one side appears on the other side. So attention layers can actually receive weird-looking windows that contain two opposite sides of an image stitched together. The source code for this model is available in PyTorch <a href="https://github.com/microsoft/Swin-Transformer/blob/main/models/swin_transformer.py">here</a>.</p> <p>A model called <strong>T2T from “Tokens-to-Token ViT: Training Vision Transformers from Scratch on ImageNet”</strong><a href="#db61">¹⁹</a> recursively tokenizes an input image to tokens. We won’t review it further not to diverge from the main topic too much.</p> <p>It’s quite evident that the one thing TNT, Swin, and T2T have in common is a somewhat <strong>hierarchical approach to patching</strong>, which does seem closer to the way humans extract information from images. It’s also not rare to see a network that uses Transformer blocks on top of CNN-extracted features, as it was done for example in LoFTR<a href="#8ed3">⁷</a>. Only time will tell if there are better ways to create a sequence from an image, but regardless, there is no doubt that patch embedding is the part of the classic ViT that calls for a lot of improvement.</p> <h3>1.2 (Optional) Positional masking</h3> <p>After the previous stage of patch embedding, we have a sequence of patches/tokens, each projected into a latent vector of length D. Now is the time to apply positional masking. However, it is very important to emphasize that this step is optional: it depends on the type of training that’s being done. <strong>The main training recipe for classification doesn’t require any masking at all </strong>and so this step is skipped completely. However, it is necessary for training <strong>the masked patch prediction task</strong> briefly mentioned in the paper.</p> <p>The motivation behind such a training regime is <strong>self-supervision.</strong> In this context, it means training the model to restore the masked patches, which are known to us, but not shown to the model. This creates a form of supervision without the need for any human-made labels. Clearly, this doesn’t mean that the model will be able to actually classify images yet, but it will have a good initialization. When we finally train for the classification task, we’ll start with a model that’s already somewhat aware of the contents of images.</p> <p>We’ll discuss it in more detail soon, but first, let’s understand the mechanics. For this type of training, some elements of the sequence are randomly selected and masked. How exactly? Knowing that masked patch prediction training is planned, we’ll initialize <strong>a parameter vector</strong> <strong>that will serve as a special [MASK] token</strong>. We’ll go through the hidden vectors we want to mask (random for each training iteration) and will overwrite their embeddings with this token. This way we are masking, or in other words hiding, some patches in the image.</p> <p>A [MASK] token is <strong>exactly the same in every masked position</strong>, meaning that the parameters are shared. It is initialized with zeros but will change throughout training together with other parameters of the model. It is not mentioned in the paper why a learnable token is used. An alternative would be to just always mask hidden vectors with zeros. It is possible that this choice was arbitrary following the “why not” logic. But now that we have to explain it backward, we can hypothesize that there can be images where zeros will resemble other embeddings too much, so perhaps there are better values for this special token and they should be learned instead.</p> <p>If you are wondering whether the mask is applied in any other layer of the model, then the answer is no, it’s not. Right after patch embeddings is the only place where positional masking happens. The purpose of this is to <strong>pre-train the model in a self-supervised manner</strong>. Such pre-training does not require any labels, only images themselves. You randomly hide parts of an image from the model, but then feed them as labels in a specific form: either as a mean pixel value or as all patch pixel values, possibly resized down. By learning to restore the hidden pixels the model is supposed to learn more meaningful representations and relationships in the data before it even gets to see the real labels.</p> <p>The data used for this stage can be images from the same labeled set you have or some other similar set of images. The authors state that <strong>a small subset is enough because they observe diminishing returns</strong> on downstream performance after 100k pretraining steps. After the pre-training stage, the head of the model is changed back to a classification head and regular supervised training begins. The reported improvement in accuracy on ImageNet compared to training from scratch is 2%.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*Lw7e2QkbB1R2Au0RW9eonw.gif"/><figcaption><em>Figure 5: Positional masking with a learnable [MASK] token [</em>Source: Image by the author.<em>]</em></figcaption></figure> <p><strong>Code:</strong></p> <p>Below embeddings obtained from the previous step are used (right after <em>Conv2d</em>).</p> <pre>batch_size, seq_length = embeddings.shape[:2]  # 1 and 196 = 14*14<br /><br /># Define a learnable [MASK] token, requires_grad is True by default<br />mask_token = torch.nn.Parameter(torch.zeros(1, 1, hidden_size))  # torch.Size([1, 1, 768])<br /><br /># Create a binary mask, choose to mask out 4th and 20th patches as an example<br />bool_masked_pos = torch.zeros((seq_length,))  # torch.Size([196])<br />bool_masked_pos[4] = 1.<br />bool_masked_pos[20] = 1.<br /><br /># Replace patch embeddings with mask token on the selected positions<br />mask_tokens = mask_token.expand(batch_size, seq_length, -1)  # torch.Size([1, 196, 768])<br />masked_positions = bool_masked_pos.unsqueeze(-1).type_as(mask_tokens)  # torch.Size([196, 1])<br />embeddings = embeddings * (1.0 - masked_positions) + mask_tokens * masked_positions  # torch.Size([1, 196, 768])<br /><br /># Compare new embeddings to [MASK] token<br />print(&#39;The 4th token matches the 20th token&#39;, torch.equal(embeddings[:, 4], embeddings[:, 20]))  # prints True<br />print(&#39;The 4th token matches the [MASK] token&#39;, torch.equal(embeddings[:, 4], mask_token.squeeze(1)))  # prints True<br /><br /># Run yoursef here: https://colab.research.google.com/github/yurkovak/ViT_notebook/blob/main/Vision_Transormer_(ViT)_under_the_magnifying_glass.ipynb</pre> <p>Note a neat implementation trick: embeddings are written as</p> <pre>embeddings = embeddings * (1.0 - masked_positions) + mask_tokens * masked_positions</pre> <p>Alternatively, it’s possible to use boolean indexing directly. PyTorch won’t have any problem differentiating something like this:</p> <pre>positions_to_mask = [4, 20]<br />embeddings[:, positions_to_mask] = mask_token.expand(batch_size, len(positions_to_mask), -1)</pre> <p><strong>Parameter count:</strong></p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*eXLYm5mNe91LRyNPBOIz1Q.jpeg"/></figure> <p>Remember that if positional masking isn’t performed then new parameters aren’t introduced.</p> <p><strong>Let’s dig deeper:</strong></p> <p><strong><em>Similarly to BERT</em></strong></p> <p>The masking we are discussing now was initially introduced in the field of <strong>NLP in BERT</strong> from “BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding”¹⁶. In this paper, the authors shared very impressive accuracy gains over the classic Transformer encoder<a href="#73bd">¹</a> and achieved <strong>state-of-the-art results across a wide range of benchmark datasets</strong>, in various tasks. All of this without making any significant changes to the architecture, only by masked language modeling and a few other types of pre-training. They also use a [MASK] token, which is ultimately just a name for a learnable parameter vector.</p> <p>Considering their success, it clearly seems very promising to make it work in ViT, so that’s what the authors of ViT tested. They even used <strong>almost the same probabilities for masking</strong>. In BERT 15% of positions are masked, while in ViT a 50% chance is used. But whenever a token is picked for masking and as the one to be restored, both papers use the [MASK] token 80% of the time, a random token 10% of the time, and leave an embedding unchanged in the remaining 10% of cases. The motivation for having a bit of unpredictability as opposed to just using the [MASK] for masking all the time is explained in BERT as follows:</p> <blockquote>“… Transformer encoder does not know which words it will be asked to predict or which have been replaced by random words, so it is forced to keep a distributional contextual representation of every input token.”¹⁶</blockquote> <p>Note that this randomness isn’t implemented in the Hugging Face model, so it’s not present in the code above either.</p> <p><strong><em>Related works</em></strong></p> <p>The procedure we are seeing now is, as the authors of ViT fairly call it, just a preliminary exploration. Many works have followed taking similar but slightly different approaches to <strong>masked image modeling </strong>(<strong>MIM</strong> for short, by an analogy to MLM, masked language modeling). For example, both MAE from “Masked Autoencoders Are Scalable Vision Learners”<a href="#5306">²⁰</a> and BEiT from “BEIT: BERT Pre-Training of Image Transformers”<a href="#bfd2">⁵</a> also try to improve downstream accuracy by reconstructing missing pixels.</p> <p>On the other hand, there is another well-established direction for self-supervised pre-training that uses <strong>self-distillation</strong>. There, instead of pixels directly, a latent vector produced by a teacher is used as a label for a student. A teacher can be for example an exponential moving average of a student, that’s why the approach is called self-distillation. One example of such a method would be a recent work “What to Hide from Your Students: Attention-Guided Masked Image Modeling”<a href="#fac1">²¹</a> where the authors question the effectiveness of choosing patches for masking completely at random. They show that some patches are too easy and so reconstructing them is less helpful than masking and reconstructing hard, kind of less boring patches. They use the teacher’s attention scores to guide which parts of an input image should be masked.</p> <p>It won’t go amiss to repeat once again that masking is one of the approaches for self-supervised pre-training, but it is <strong>not required for the usual supervised classification</strong> which is actually the main task of the ViT. So let’s get back on track and start the next section where we’ll learn about an interesting trick used for classification.</p> <h3>1.3 [CLS] token</h3> <p>So far we’ve seen one special learnable token, here comes another one. In this section, we are going to talk about a [CLS] token, another concept inherited from BERT. But unlike [MASK], this token will always be present in the model. Remember that [MASK] is used to completely replace embeddings of certain patches? Instead, [<strong>CLS] has a special position in a sequence</strong>, that is, it always precedes all the other embeddings and increases a sequence length by 1. This token is also a learnable parameter vector and it’s stacked right before all the other embeddings we’ve obtained until now.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*MPAxRmg05bs2eMQYh_8ZZw.gif"/><figcaption><em>Figure 6: Concatenation of learnable parameters of the [CLS] token [</em>Source: Image by the author.<em>]</em></figcaption></figure> <p><strong>Code:</strong></p> <pre>cls_token = torch.nn.Parameter(torch.zeros(1, 1, hidden_size))  # torch.Size([1, 1, 768])<br />cls_tokens = cls_token.expand(batch_size, -1, -1)  # torch.Size([1, 1, 768])<br />embeddings = torch.cat((cls_tokens, embeddings), dim=1)  # torch.Size([1, 197, 768])<br />new_seq_length = embeddings.shape[1]<br />print(new_seq_length)  # prints 197 = 14*14 + 1<br /><br /># Run yoursef here: https://colab.research.google.com/github/yurkovak/ViT_notebook/blob/main/Vision_Transormer_(ViT)_under_the_magnifying_glass.ipynb</pre> <p><strong>Parameter count:</strong></p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*eXLYm5mNe91LRyNPBOIz1Q.jpeg"/></figure> <p><strong>Let’s dig deeper:</strong></p> <p>If you haven’t seen it in the past, then I bet you are not sure why it is even needed. Let’s fast-forward for a moment to the end of the network. Spoiler alert, all the layers in the model will be dealing with a sequence of latent vectors, up to the very end when it’s time to attach a classification head. The question is, how can we use all vectors together and where should the classification head be attached? There are <strong>a few ways to aggregate all the sequence elements together</strong>. One of the commonly used methods is Global Average Pooling (GAP), which lies in creating an average vector and using it as an input to a classification fully-connected layer. But [CLS] token is another, more unusual way that can serve the same purpose. It was initially introduced in BERT<a href="#cd8b">¹⁶</a> and consequently used in ViT. Let’s try to understand how it can aid a classification head.</p> <p>To state the obvious, the name [CLS] stands for “classification”. When we reach self-attention layers, you’ll see that each hidden vector will be able to attend to other elements according to their relevance. There, all the other hidden vectors that originally corresponded to image patches will only “care” about themselves, each position will gather the information that will help its patch to be represented better. But the [CLS] token has a special nature, it doesn’t store any knowledge of its own. Instead, it will collect/<strong>aggregate information useful for the classification task</strong>.</p> <p>The way to make this concept work is by encouraging this behavior with the architecture. To be specific, all the other encoding vectors returned by the last layer of the encoder will be <strong>discarded, all except the encoding on the <em>0-th</em> position</strong> corresponding to the [CLS] token. Only the encoding of the [CLS] token from the last encoder layer will be used by a classification head. This way, this sequence element will be forced to learn to encapsulate information useful for classification.</p> <p>But let’s not rush too much to the last layers, we have the whole encoder (part 2) ahead of us. Just remember for now about the special purpose of this new sequence element.</p> <h3>1.4 Positional Embedding (PE)</h3> <p>We’ve reached the last part of embedding, a positional one. It is quite an intriguing concept, because it’s not something intrinsic to CNNs, and not even to RNNs for that matter. Instead, it is pretty <strong>specific to transformers</strong> for one particular reason: they contain permutation-invariant MHSA layers which will be covered in part 2. It means that if you permute elements that such a layer accepts, the result of it won’t change. This property of course has its advantages, but considering we’ve just split an image into non-overlapping patches and flattened them into one long 1D sequence, we do want to preserve some information about the order. So positional embedding is supposed to do exactly that.</p> <p>In practice, the solution used in ViT is surprisingly trivial. We’ll let the network learn <strong>a unique parameter vector for each position</strong>, and that’s how layers in the rest of the network will be able to differentiate where each token is coming from. In other words, yet another special parameter have to be introduced, this time with a shape that contains sequence length. Then, this parameter tensor will be added to the current embeddings. That’s all. It can be said that this trick works “out of the box”, there is no need to dedicate a loss term to it. The model is able to<strong> learn a meaningful positional embedding as a byproduct of training for the main task</strong>, simply because positional information is helpful. And in this section, we’ll investigate and visualize what exactly is learned in these parameters.</p> <p>Note that none of the parameters we’ve introduced previously depended on the sequence length, so in case you’ve lost track of the number of elements we have in the sequence, now is a good time to reiterate. Recall that we preceded the sequence with an extra [CLS] token in the previous step. Thus, the length is 1 plus the total number of patches in the non-overlapping grid, <strong>(1 + H/P x W/P)</strong>, where (H x W) is the initial input image resolution and P is the used patch size.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*bQlDXCNRiU5yiMK-i7NfEg.png"/><figcaption><em>Figure 7: Summation of learnable positional embedding [</em>Source: Image by the author.<em>]</em></figcaption></figure> <p><strong>Code:</strong></p> <pre># Initialize position_embeddings parameters and add to the sequence embeddings<br />position_embeddings = torch.nn.Parameter(torch.zeros(1, new_seq_length, hidden_size))  # torch.Size([1, 197, 768])<br />embeddings = embeddings + position_embeddings  # torch.Size([1, 197, 768])<br /><br /># Run yoursef here: https://colab.research.google.com/github/yurkovak/ViT_notebook/blob/main/Vision_Transormer_(ViT)_under_the_magnifying_glass.ipynb</pre> <p><strong>Parameter count:</strong></p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*SSPcqfT-Es19ioOa4gWx7g.jpeg"/></figure> <p><strong>Let’s dig deeper:</strong></p> <p><strong><em>2D structure</em></strong></p> <p>One more time we observe here how <strong>a spatial structure of an image is ignored</strong>. Each patch has a 2D coordinate in the original image, X and Y on a 14x14 grid, yet, the positional embedding is 1D. To clarify, the positional embedding of each token isn’t one-dimensional, it has D values, but on the image level, there are no heuristics that would explicitly convey to the model the 2D positioning of patches and the corresponding relationships between them. Such heuristics would imply that, for example, two patches in the <em>4-th</em> column but from different rows share the same embedding for the Y axis.</p> <p>Instead, in 1D, each position in the embedding is free to be completely unique, the authors of ViT just let the model learn everything it needs. Why? This is not a random design choice, but rather something that was properly ablated in the paper. They tested many options: 1D, 2D, no positional embedding at all, and even a more elaborate relative one. The only significant gap in the accuracy of almost 3% was observed when comparing models <strong>with and without any position embedding</strong>, <strong>while the used strategy itself didn’t matter much</strong>. So considering these results, it definitely makes sense to stick to 1D for simplicity.</p> <p>It’s worth noting that it’s also stated in the paper that a model learns to represent a 2D image topology with 1D embeddings anyway. The authors speculate that it might be because a 14x14 grid isn’t that hard to “figure out” due to a relatively small grid resolution. They provide a cool figure to prove the claim.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/0*W1AIWFLE-sb-dbwu"/><figcaption><em>Figure 8: From “</em>An Image is Worth 16x16 Words: Transformers for Image Recognition at Scale.<em>”</em><a href="#3fef"><em>²</em></a><em> captioned as “Position embeddings of models trained with different hyperparameters.”</em></figcaption></figure> <p>Let’s try to understand what’s going on here. What you see is a cosine similarity between all-to-all positional embeddings of a model trained with three different regimes. That is, to navigate the chart you should pick one plot from these three, look at some position <em>(i, j),</em> and find a similarity map. On this small square, a color at each position <em>(k, m)</em> tells you how similar the positional embedding of the patch <em>(i, j) </em>is to the positional embedding of the patch <em>(k, m)</em>.</p> <p>Since it is emphasized that <strong>different training hyperparameters lead to different patterns</strong>, it would be interesting to take a look at such a figure for a pre-trained model available in Hugging Face. The source code for generating the charts from the paper wasn’t open-sourced, so let’s write it ourselves and try to reproduce the visualization:</p> <pre>def similarity_one_to_all(pos_embedding: torch.tensor, all_embeddings: torch.tensor) -&gt; np.ndarray:<br />    cos_sim = torch.nn.CosineSimilarity(dim=-1)<br />    similarities = []<br />    # Compare the given pos_embedding to all other embeddings one by one<br />    for i in range(len(all_embeddings)):<br />        similarity = cos_sim(pos_embedding, all_embeddings[i])  # one values<br />        similarities.append(similarity)<br />    return np.array(similarities)<br /><br /><br />def similarity_all_to_all(pos_embeddings: torch.tensor, grid_h: int, grid_w: int) -&gt; List[List]:<br />    &quot;&quot;&quot;<br />    Compute similarity between  positional embeddings, all to all<br />    :param pos_embeddings:  embeddings of shape (sequence_length x D),<br />                            sequence_length must be equal to grid_h * grid_w<br />    :param grid_h:          number of patches along Y axis<br />    :param grid_w:          number of patches along X axis<br />    :return:                grid_h x grid_w array of similarity heatmaps,<br />                            where similarity_heatmaps[i][j] stores similarity between embedding<br />                            for the patch at (i, j) to all the other embeddings<br />    &quot;&quot;&quot;<br />    # Reshape pos. embeddings into a grid<br />    pos_embeddings_grid = pos_embeddings.reshape((grid_h, grid_w, -1))<br />    similarity_heatmaps = []<br />    # Go over patches rows<br />    for i in range(grid_h):<br />        row_i_heatmaps = []<br />        # Go over patches columns<br />        for j in range(grid_w):<br />            # Compare pos. embedding (i, j) to all other embeddings and reshape similarities into a heatmap<br />            similarities = similarity_one_to_all(pos_embeddings_grid[i, j], pos_embeddings)<br />            row_i_heatmaps.append(similarities.reshape((grid_h, grid_w)))<br />        similarity_heatmaps.append(row_i_heatmaps)<br /><br />    return similarity_heatmaps<br /><br /><br /># Load pre-trained ViT-B/16 and get its position embedding<br />model = ViTForImageClassification.from_pretrained(&quot;google/vit-base-patch16-224&quot;)<br />trained_pos_embeddings_all = model.vit.embeddings.position_embeddings[0].detach()  # torch.Size([197, 768])<br /><br /># Discard batch dimension and [CLS] token position<br />image_pos_embeddings = trained_pos_embeddings_all[1:]  # torch.Size([196, 768])<br />grid_h, grid_w = im_h // patch_size, im_w // patch_size<br /><br /># Compute similarities and visualize<br />similarity_heatmaps = similarity_all_to_all_vectorized(image_pos_embeddings, grid_h, grid_w)<br />visualize_heatmaps(similarity_heatmaps, grid_h, grid_w)<br /><br /># Run yoursef here: https://colab.research.google.com/github/yurkovak/ViT_notebook/blob/main/Vision_Transormer_(ViT)_under_the_magnifying_glass.ipynb</pre> <p>In this snippet, you can find the function for computing similarities, but the plotting function is omitted due to irrelevance to the explanation. You can find it, as well as a much shorter vectorized form of <em>similarity_all_to_all(…)</em> in <a href="https://colab.research.google.com/github/yurkovak/ViT_notebook/blob/main/Vision_Transormer_(ViT)_under_the_magnifying_glass.ipynb">the Colab notebook</a>. The code produces the plot below, except for the zoom-in which was added manually to add clarity:</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*1kl1m4UD_07pXzEIUrozyA.png"/><figcaption><em>Figure 9: Similarity of the learned positional embeddings of the pre-trained model from Hugging Face </em><a href="https://huggingface.co/google/vit-base-patch16-224">“google/vit-base-patch16–224”</a><em>, all-to-all; the highlighted similarity map is the similarity of PE of the patch at (11, 9) to the PEs of all other patches [</em>Source: Image by the author.<em>]</em></figcaption></figure> <p>As you can see, each positional embedding is the most similar to itself (obviously), but it is also very similar to the blob around it. This means that <strong>the model embeds spatially neighboring regions with very similar vectors</strong> which is a desirable behavior. Surprisingly, for each position embedding, there are also other regions it’s highly similar to, that is, there are more bright blobs than one. It indicates that similar positional embedding vectors are used for various regions.</p> <p>In case you haven’t noticed, for visualization purposes, we had to discard the positional embedding of the [CLS] token and compared only the PEs of image patches, one to another. But what about the PE of the [CLS] token? After all, it is not located within an image, so it doesn’t have a 2D coordinate on the patches grid. It’s interesting to see whether it ended up similar to any other pos. embeddings. The logic tells us that it shouldn’t be considering its special nature. Indeed, in the plot below, you can see that the learned values are different from all other pos. embedding, the similarity is on average 0, which means that <strong>the PE of the [CLS] token is almost orthogonal to the PEs of all the real patches</strong>. Intuitively, it means that the similarity between the PE of the [CLS] token and the PE of any other given patch is as small as it can be, namely, they are very different. This is due to the fact that the position of the [CLS] token is special, unlike any of the positions within the image grid.</p> <pre># Get positional embedding that corresponds to the [CLS] token<br />cls_pos_pos_embedding = trained_pos_embeddings_all[0]  # 0-th position, torch.Size([768])<br /><br /># Visualize similarity between [CLS] token PE to all other PE-s<br />cls_pos_to_all_similarity = similarity_one_to_all(cls_pos_pos_embedding, image_pos_embeddings)<br />plt.figure(figsize=(10, 10))<br />plt.imshow(cls_pos_to_all_similarity.reshape((grid_h, grid_w)), vmin=-1, vmax=1)<br />cbar = plt.colorbar(aspect=25, ticks=[-1, 1], label=&#39;Cosine similarity&#39;)<br />cbar.set_label(label=&#39;Cosine similarity&#39;, fontsize=24)<br />plt.xticks([0, grid_w - 1], labels=[1, grid_w], fontsize=24)<br />plt.yticks([0, grid_h - 1], labels=[1, grid_h], fontsize=24)<br />plt.show()<br /><br /># Run yoursef here: https://colab.research.google.com/github/yurkovak/ViT_notebook/blob/main/Vision_Transormer_(ViT)_under_the_magnifying_glass.ipynb</pre> <figure><img alt="" src="https://cdn-images-1.medium.com/max/597/0*2YTH6P5gyyAupBZO"/><figcaption><em>Figure 10: Similarity of the positional embedding of the [CLS] token to the position embeddings of all other patches [</em>Source: Image by the author.<em>]</em></figcaption></figure> <p><strong><em>Fixed position encodings</em></strong></p> <p>Note that a learnable positional embedding is one of the discrepancies between ViT and the Transformer<a href="#73bd">¹</a> encoder. <strong>The NLP Transformer uses a fixed formula for this purpose.</strong> The formula calculates sine and cosine with a unique wavelength for each position <em>i</em> in the D-dimensional encoding vector (sine for even <em>i</em>-s, cosine for odd). This function was chosen in “Attention is all you need”<a href="#73bd">¹</a> for the nice properties that:</p> <ul><li><strong>the further </strong>the elements, <strong>the smaller</strong> the similarity;</li><li>it is <strong>symmetric</strong>;</li><li>the <em>(j + k)-th</em> position can be <strong>expressed as a linear function</strong> of the <em>j-th</em> encoding:</li></ul> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*gF_XWCZtddypSpqrW78s_w.jpeg"/></figure> <p>You can find the exact formula and proof in <a href="https://kazemnejad.com/blog/transformer_architecture_positional_encoding/">this great article</a>, and we’ll take advantage of the code we have and v<strong>isualize the all-to-all similarities for such fixed embedding</strong>. For this, we’ll use an implementation of the cosine-sine formula from <a href="https://pytorch.org/tutorials/beginner/transformer_tutorial.html">a PyTorch tutorial</a>.</p> <pre>def get_fixed_pos_encodings(sequence_length: int, D: int) -&gt; torch.tensor:<br />    &quot;&quot;&quot;<br />    Create positional encodings according to the formula used for Transformer in<br />    &quot;Attention Is All You Need&quot;<br />    &quot;&quot;&quot;<br />    position = torch.arange(sequence_length).unsqueeze(1)<br />    div_term = torch.exp(torch.arange(0, D, 2) * (-math.log(10000.0) / D))<br />    pe = torch.zeros(sequence_length, D)<br />    pe[:, 0::2] = torch.sin(position * div_term)<br />    pe[:, 1::2] = torch.cos(position * div_term)<br />    return pe<br /><br /><br /># Create fixed positional encoding and visualize similarities<br />fixed_pos_encodings = get_fixed_pos_encodings(grid_h * grid_w + 1, hidden_size)  # torch.Size([197, 768])<br />similarity_heatmaps_fixed = similarity_all_to_all(fixed_pos_encodings[1:], grid_h, grid_w)<br />visualize_heatmaps(similarity_heatmaps_fixed, grid_h, grid_w)<br /><br /># Run yoursef here: https://colab.research.google.com/github/yurkovak/ViT_notebook/blob/main/Vision_Transormer_(ViT)_under_the_magnifying_glass.ipynb</pre> <figure><img alt="" src="https://cdn-images-1.medium.com/max/922/0*nqw4lVvYH29ZXyp7"/><figcaption><em>Figure 11: Similarity of all-to-all the positional encodings defined by the sine and cosine formula from “Attention Is All You Need”</em><a href="#73bd"><em>¹</em></a><em> [</em>Source: Image by the author.<em>]</em></figcaption></figure> <p>As you can see, the pattern is very different from the one learned by ViT. In particular, the similarity is the highest between patches in the same row and <strong>the spatial structure isn’t apparent at all</strong>. It comes as no surprise, considering the formula was picked for text sequences and wasn’t meant to deal with images. To understand why the similarities of fixed encoding are the way they are, let’s take a look at the values themselves.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/730/0*Ig1OcuSxMNTbN9Z_"/><figcaption><em>Figure 12: Top: Absolute values of Transformer’s positional encodings defined by the sine and cosine formula from “Attention Is All You Need”¹; Bottom: of the leared position embeddings of a pre-trained ViT </em><a href="https://huggingface.co/google/vit-base-patch16-224">“google/vit-base-patch16–224”</a> <em>[</em>Source: Image by the author.<em>]</em></figcaption></figure> <p>As usual with learned weights, the values of ViT are hard to interpret. But the values of the fixed encoding of the Transformer pretty much explain their similarity map in Figure 11: vectors are the most similar for the adjacent sequence elements. This means that with fixed encoding vertically neighboring patches would get very different PE parameters. All in all, apparently, <strong>the learned embedding suits ViT much better</strong>, since it naturally learns a 2D structure without any special heuristics.</p> <p><strong><em>Placement in the network</em></strong></p> <p>The last architecture design to consider is the placement of positional embedding in the network. We already know that it is added once, shortly after linear patch embeddings. But what are the alternatives? One is to change the placement from the stem where we are now, to <strong>more locations</strong>. The authors of the paper test the addition of positional embeddings in every layer, with a new set of learnable parameters or with shared weights. The difference in accuracy compared to the baseline approach was negligible. It might be thanks to the skip connections that, as we’ll see in part 2, go through all the layers, allowing propagation of the information obtained by the current layer.</p> <p>A similar and related discussion was raised in <a href="https://github.com/tensorflow/tensor2tensor/issues/1591">this GitHub issue</a>. <strong>Why are the positional embeddings added</strong> to the patch embeddings when it is often the case in neural networks that the concatenation of feature maps/vectors yields better performance than adding them? While I’m not aware of any paper that would directly ablate concatenation of patch embeddings and positional embedding, there are a few potential reasons to resort to addition anyway. Firstly, it saves on parameters in the layer to come, meaning that we’d need bigger neurons to process a result of concatenation. Secondly, even with the summation, the network can easily learn to dedicate some positions within a vector of length D to a stronger signal from positional embedding, and other positions within a vector to a stronger signal from patch embedding. And the third hypothetical reason is in my opinion the most interesting one. It lies in the fact that it might be helpful to encode some patches differently depending on the position they are coming from. To rephrase, an embedding of a patch, which is the same regardless of its location, will be altered by positional embedding using summation, resulting in unique position-dependant features.</p> <h3>1.5 Dropout (DP)</h3> <p>We’ve reached the last layer in the embeddings stage, Dropout<a href="#ab79">²²</a>. This layer is widely used for all kinds of deep neural networks to prevent overfitting, so there is a high chance that you are already familiar with it. Let’s quickly recall the idea behind this trick.</p> <p>The problem that Dropout is meant to solve is complex co-adaptations that build between neurons during training. That is, during back-propagation, <strong>specific weights may learn to correct mistakes other neurons cause</strong>. The authors claim that such co-adaptations generalization poorly to new data, and as a result, lead to overfitting. The proposed solution is to drop out random neurons during training, different ones each iteration. There is no need to literally delete a neuron and its weights. Instead, it can be <strong>implemented by simply zeroing out</strong> some neurons’ outputs, as if they weren’t present in the network at the current iteration. Since all the neurons are eventually used at test time, this can be perceived as a form of combining exponentially many different architectures.</p> <p>As you can see, Dropout is one of those layers that have different behaviors at train and test times. This creates the following caveat: think about the next layer, the one that relies on inputs that had Dropout applied to them. During training, this <strong>following layer adapts to certain value distributions</strong>. Let’s take some value x at position <em>(i, j)</em>, an output of a certain neuron. We know its expected value at test time, considering that nothing is zeroed out:</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*4hgYUllqo7IAnSDDZWk6Xw.jpeg"/></figure> <p>But its expected value during training is</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*TtRzDbhXMSJisvQtQ1_Tyw.jpeg"/></figure> <p>where p is the probability of <em>x</em> <em>at</em> <em>(i, j) </em>to be zeroed out.</p> <p>So that’s what the next layer will in fact learn to expect. However, we can see that the expected value of <em>x</em> <em>at</em> <em>(i, j)</em> at test time will be bigger, so we must scale it back by the factor of <em>(1 — p)</em>. Alternatively, the process can be optimized for inference by applying <strong>inverse scaling during training</strong>. In particular, if during training together with zeroing out some values, we divide all values by <em>(1 — p)</em>, then at train time we get the following expectation:</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*WPJM8CEKIqiiW-rMAszXkQ.jpeg"/></figure> <p>This allows the next layer to always “work” with the same distribution of inputs, both during training and during inference.</p> <p>That being said, in PyTorch, there is no need to write it manually, it’s implemented in <em>torch.nn.DropOut</em>, but it’s always good to remember what happens under the hood.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*v6LOkTKfvvaxTkrp3abWkg.gif"/><figcaption><em>Figure 13: Application of Dropout to embeddings, train mode [</em>Source: Image by the author.<em>]</em></figcaption></figure> <p><strong>Code:</strong></p> <p>The code below applies Dropout to the embeddings. But unlike with other layers, it does so both in train and in eval mode. Outputs obtained in both modes are saved into separate variables.</p> <p>Don’t get confused by the double application, during inference Dropout should be applied only once, in the mode that corresponds to the current stage, training, or evaluation. But for debugging purposes, we’ll need both, because we are going to actually compare values and make sure that the results hold true to the formula explained above.</p> <pre># Define the DropOut layer and apply it in the train mode (the default mode)<br />dropout_prob = 0.1<br />dropout = torch.nn.Dropout(dropout_prob)<br />embeddings_after_train_dp = dropout(embeddings)  # torch.Size([1, 197, 768])<br /><br /># Apply DropOut to the same input embeddings, but in eval mode<br />dropout.eval()<br />with torch.no_grad():<br />    embeddings_afer_eval_dp = dropout(embeddings)<br /><br /># Run yoursef here: https://colab.research.google.com/github/yurkovak/ViT_notebook/blob/main/Vision_Transormer_(ViT)_under_the_magnifying_glass.ipynb</pre> <p>Using <em>embeddings_after_train_dp</em> and <em>embeddings_afer_eval_dp</em>, let’s check a few things for both modes:</p> <ul><li><strong>how many zeros</strong> there are in the tensor before and after the operation, in % from the total number of values;</li><li>what <strong>the actual values</strong> are.</li></ul> <pre># Count zero values percentage before and after the operation<br />with torch.no_grad():<br />    get_zeros_percentage = lambda t: round(((t == 0.).sum() / t.numel()).item() * 100, 1)<br />    print(f&#39;Percentage of zeros BEFORE applying dropout: {get_zeros_percentage(embeddings)}%&#39;)<br />    print(f&#39;Percentage of zeros AFTER applying dropout (train): {get_zeros_percentage(embeddings_after_train_dp)}%&#39;)<br />    print(f&#39;Percentage of zeros AFTER applying dropout (eval): {get_zeros_percentage(embeddings_afer_eval_dp)}%\n&#39;)<br /><br /># Check values before and after the operation<br />with torch.no_grad():<br />    embeddings_afer_eval_dp = dropout(embeddings)<br />    print(&#39;Values of input embeddings:\n&#39;, embeddings[0, :4, 0].detach())<br />    print(&#39;Values of embeddings after DP (train):\n&#39;, embeddings_after_train_dp[0, :4, 0].detach())<br />    print(&#39;Values of input embeddings scaled with 1/(1 - p):\n&#39;, embeddings[0, :4, 0]/(1 - dropout_prob))<br />    print(&#39;Values of embeddings after DP (eval):\n&#39;, embeddings_afer_eval_dp[0, :4, 0].detach())<br /><br /># Run yoursef here: https://colab.research.google.com/github/yurkovak/ViT_notebook/blob/main/Vision_Transormer_(ViT)_under_the_magnifying_glass.ipynb</pre> <p>The result will vary due to randomness, but here is an example log of these prints:</p> <pre>Percentage of zeros BEFORE applying dropout: 1.5%<br />Percentage of zeros AFTER applying dropout (train): 11.3%<br />Percentage of zeros AFTER applying dropout (eval): 1.5%<br /><br />Values of input embeddings:<br /> tensor([0.0000, 0.5994, 0.5994, 0.5994])<br />Values of embeddings after DP (train):<br /> tensor([0.0000, 0.6660, 0.6660, 0.0000])<br />Values of input embeddings scaled with 1/(1 - p):<br /> tensor([0.0000, 0.6660, 0.6660, 0.6660])<br />Values of embeddings after DP (eval):<br /> tensor([0.0000, 0.5994, 0.5994, 0.5994])</pre> <p>As you can see, in the train-mode output, there are roughly <em>(dropout_prob * 100) %</em> zeros. There is actually a bit more due to some naturally occurring zeros in the input, that is, <strong>not all zeros are induced by DP.</strong> The values that remain are equal to input_value divided by <em>(1 — p)</em>. Conversely, in the eval mode, Dropout does absolutely nothing to the input.</p> <p><strong>Parameter count:</strong></p> <p>Adding Dropout doesn’t introduce any new parameters.</p> <p><strong>Let’s dig deeper:</strong></p> <p><strong><em>The used probabilities</em></strong></p> <p>The dropout probability <strong><em>p</em> is a hyperparameter</strong> that must be set manually. The authors of ViT use <em>p = 0</em> for training on JFT-300M<a href="#de61">²³</a>, an internal Google dataset with more than 300 million images. Such probability makes it equivalent to not using Dropout at all. However, <em>p = 0.1</em> is used for all model sizes when training from scratch on ImageNet<a href="#03dc">²⁴</a>, both IN-1K (with roughly 1 million images) and IN-21K (with roughly 14 million images). They write that, with this dataset, strong regularization was crucial.</p> <p><strong><em>Placement in the network</em></strong></p> <p>If you scroll up to the “Achitecture” section and look at the list of layers, you may notice that there will be more locations in the network where the Dropout layer is used. The authors state that they apply Dropout “after every dense layer except for the qkv-projections and directly after adding positional embedding to patch embeddings.”². If you have prior experience with various CNN architectures, you can recall that <strong>it’s not common to find a Dropout layer in CNNs anywhere except in a classification head</strong>. The reason for this lies in the way feature maps are produced in CNNs. There, due to ubiquitous usage of <em>stride = 1 </em>and also due to spatially shared convolutional weights, zeroing out a random value neither fully removes access to an input region nor deletes the whole neuron altogether. As a result, various structured forms of Dropout were introduced in the past, e.g. DropBlock<a href="#411e">²⁵</a>, DropPath<a href="#f0af">²⁶</a>, DropConnect<a href="#7cb2">²⁷</a>, etc. When applied on feature maps throughout the network, these have much more effect on reducing overfitting than Dropout.</p> <p>But let’s get back to ViT and think for a moment. Which neurons can we say get deleted when we temporarily zero out a value in the (H/P x W/P + 1) x D embeddings tensor? It is important to emphasize that <strong>Dropout was initially proposed for networks that consist of fully connected layers</strong> and operate on a 1D vector, not on a sequence of vectors. In such a neural network, zeroing out one value indeed imitates a deletion of the whole neuron used to produce it.</p> <p>But the embeddings we have at this stage are 2D. Recall that they are the result of the summation of patch embeddings and position embeddings. <strong>Zeroing out a value in this tensor completely removes access to a corresponding value among positional embedding parameters.</strong> As for the linear patch embedding layer (Conv2d) from section 1.1, none of its kernels are “gone” completely. To clarify, even if <em>x at</em> <em>(i, j)</em> is set to 0, some other <em>x at</em> <em>(i + k, j)</em> likely isn’t, so the kernel that’s used to produce the <em>j-th</em> feature in an embedding of length <em>D</em> remains.</p> <p>That’s why a Dropout layer in this particular placement in the network is sometimes called <strong>positional dropout</strong>. As for the other locations in the network, some vision transformers do favor a structured form of Dropout, e.g. DropPath²⁶ used for Swin transformer<a href="#cf8f">⁴</a>.</p> <h3>Summary</h3> <p>This wraps up the embedding stage of the ViT and part 1 of the article. In this part, we learned which operations are applied to an image before it reaches encoder blocks. We saw what’s done to represent an image as a sequence and figured out the mechanics behind a few special tokens. We also talked through each step in depth: a bit of history, motivation, some nice properties, issues, and further research directions.</p> <p>If you would like to go over all the animations together, it is convenient to do it in the <a href="https://colab.research.google.com/github/yurkovak/ViT_notebook/blob/main/Vision_Transormer_(ViT)_under_the_magnifying_glass.ipynb"><strong>Colab Notebook</strong></a>. Let’s also do <strong>a short recap of what we have so far:</strong></p> <ul><li>An image is convolved with a big kernel and stride equal to the kernel size. This is equivalent to splitting an image into patches and applying a linear projection.</li><li>The resulting feature map is flattened into a sequence. For self-supervised pre-training, some elements in the sequence may be masked at this stage with a learnable [MASK] token.</li><li>The sequence is prepended with a learnable parameter vector dedicated to a [CLS] token.</li><li>Positional embedding is added to the sequence, each token gets a unique parameter vector to learn its position.</li><li>Dropout is applied to the embedding.</li></ul> <p>As for <strong>the total number of parameters</strong> in the embedding stage, as you can see in Figure 14, most of them are concentrated in the linear projection layer, i.e. patch embedding.</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/804/1*Ppg8_EpEAm_5qVYXHafXIg.png"/><figcaption>Figure 14: Parameters in the embedding part of the network [Source: Image by the author.]</figcaption></figure> <p><strong>In the next part of the article we will dig into the building blocks of the encoder</strong>, including multi-head self-attention, and in part 3 will discuss the classification head and everything that goes beyond the architecture: training, inference, model sizes, some general model statistics, etc. Stay tuned!</p> <blockquote><em>If you learned something new, please consider </em><strong><em>clapping and sharing</em></strong><em> with friends, so that more people can benefit from it.</em></blockquote> <h3>Acknowledgments</h3> <p>Major thanks go to Professor Ran El-Yaniv, Anton Yurkov, Omri Puny, Natan Bagrov, Ido Shahaf, and Ofri Masad for taking the time to review the article and for contributing to making it better, more clear, and informative.</p> <h3>References</h3> <ol><li>Vaswani, A., Shazeer, N., Parmar, N., Uszkoreit, J., Jones, L., Gomez, A. N., Kaiser, L., &amp; Polosukhin, I. (2017). <strong>Attention Is All You Need.</strong> <a href="https://arxiv.org/abs/1706.03762"><em>arXiv:1706.03762</em></a></li><li>Dosovitskiy, A., Beyer, L., Kolesnikov, A., Weissenborn, D., Zhai, X., Unterthiner, T., Dehghani, M., Minderer, M., Heigold, G., Gelly, S., Uszkoreit, J., &amp; Houlsby, N. (2020). <strong>An Image is Worth 16x16 Words: Transformers for Image Recognition at Scale.</strong> <a href="https://arxiv.org/abs/2010.11929"><em>arXiv:2010.11929</em></a></li><li>Carion, N., Massa, F., Synnaeve, G., Usunier, N., Kirillov, A., &amp; Zagoruyko, S. (2020). <strong>End-to-End Object Detection with Transformers.</strong> <a href="https://arxiv.org/abs/2005.12872"><em>arXiv:2005.12872</em></a></li><li>Liu, Z., Lin, Y., Cao, Y., Hu, H., Wei, Y., Zhang, Z., Lin, S., &amp; Guo, B. (2021). <strong>Swin Transformer: Hierarchical Vision Transformer using Shifted Windows.</strong> <a href="https://arxiv.org/abs/2103.14030"><em>arXiv:2103.14030</em></a></li><li>Bao, H., Dong, L., Piao, S., &amp; Wei, F. (2021). <strong>BEiT: BERT Pre-Training of Image Transformers.</strong> <a href="https://arxiv.org/abs/2106.08254"><em>arXiv:2106.08254</em></a></li><li>Caron, M., Touvron, H., Misra, I., Jégou, H., Mairal, J., Bojanowski, P., &amp; Joulin, A. (2021). <strong>Emerging Properties in Self-Supervised Vision Transformers.</strong> <a href="https://arxiv.org/abs/2104.14294"><em>arXiv:2104.14294</em></a></li><li>Sun, J., Shen, Z., Wang, Y., Bao, H., &amp; Zhou, X. (2021). <strong>LoFTR: Detector-Free Local Feature Matching with Transformers.</strong> <a href="https://arxiv.org/abs/2104.00680"><em>arXiv:2104.00680</em></a></li><li>Fang, Z., Wang, J., Hu, X., Liang, L., Gan, Z., Wang, L., Yang, Y., &amp; Liu, Z. (2021). <strong>Injecting Semantic Concepts into End-to-End Image Captioning.</strong> <a href="https://arxiv.org/abs/2112.05230"><em>arXiv:2112.05230</em></a></li><li>Guo, M., Cai, J., Liu, Z., Mu, T., Martin, R. R., &amp; Hu, S. (2020). <strong>PCT: Point cloud transformer.</strong> <a href="https://arxiv.org/abs/2012.09688"><em>arXiv:2012.09688</em></a></li><li>Jiang, Y., Chang, S., &amp; Wang, Z. (2021). <strong>TransGAN: Two Pure Transformers Can Make One Strong GAN, and That Can Scale Up.</strong> <a href="https://arxiv.org/abs/2102.07074"><em>arXiv:2102.07074</em></a></li><li>Deng, Y., Tang, F., Dong, W., Ma, C., Pan, X., Wang, L., &amp; Xu, C. (2021). <strong>StyTr²: Image Style Transfer with Transformers.</strong> arXiv. <a href="https://arxiv.org/abs/2105.14576"><em>arXiv:2105.14576</em></a></li><li>Ramesh, A., Pavlov, M., Goh, G., Gray, S., Voss, C., Radford, A., Chen, M., &amp; Sutskever, I. (2021). <strong>Zero-Shot Text-to-Image Generation.</strong> <a href="https://arxiv.org/abs/2102.12092"><em>arXiv:2102.12092</em></a></li><li>Zhang, Q., &amp; Yang, Y. (2021). <strong>ResT: An Efficient Transformer for Visual Recognition.</strong> <a href="https://arxiv.org/abs/2105.13677"><em>arXiv:2105.13677</em></a></li><li>Li, Y., Yuan, G., Wen, Y., Hu, J., Evangelidis, G., Tulyakov, S., Wang, Y., &amp; Ren, J. (2022). <strong>EfficientFormer: Vision Transformers at MobileNet Speed.</strong> <a href="https://arxiv.org/abs/2206.01191"><em>arXiv:2206.01191</em></a></li><li>Pan, J., Bulat, A., Tan, F., Zhu, X., Dudziak, L., Li, H., Tzimiropoulos, G., &amp; Martinez, B. (2022). <strong>EdgeViTs: Competing Light-weight CNNs on Mobile Devices with Vision Transformers.</strong> <a href="https://arxiv.org/abs/2205.03436"><em>arXiv:2205.03436</em></a></li><li>Devlin, J., Chang, M., Lee, K., &amp; Toutanova, K. (2018). <strong>BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding.</strong> <a href="https://arxiv.org/abs/1810.04805"><em>arXiv:1810.04805</em></a></li><li>Park, N., &amp; Kim, S. (2022). <strong>How Do Vision Transformers Work?</strong> <a href="https://arxiv.org/abs/2202.06709"><em>arXiv:2202.06709</em></a></li><li>Han, K., Xiao, A., Wu, E., Guo, J., Xu, C., &amp; Wang, Y. (2021). <strong>Transformer in Transformer.</strong> <a href="https://arxiv.org/abs/2103.00112"><em>arXiv:2103.00112</em></a></li><li>Yuan, L., Chen, Y., Wang, T., Yu, W., Shi, Y., Jiang, Z., Tay, F. E., Feng, J., &amp; Yan, S. (2021). <strong>Tokens-to-Token ViT: Training Vision Transformers from Scratch on ImageNet.</strong> <a href="https://arxiv.org/abs/2101.11986"><em>arXiv:2101.11986</em></a></li><li>He, K., Chen, X., Xie, S., Li, Y., Dollár, P., &amp; Girshick, R. (2021). <strong>Masked Autoencoders Are Scalable Vision Learners.</strong> <a href="https://arxiv.org/abs/2111.06377"><em>arXiv:2111.06377</em></a></li><li>Kakogeorgiou, I., Gidaris, S., Psomas, B., Avrithis, Y., Bursuc, A., Karantzalos, K., &amp; Komodakis, N. (2022). <strong>What to Hide from Your Students: Attention-Guided Masked Image Modeling.</strong> <a href="https://arxiv.org/abs/2203.12719"><em>arXiv:2203.12719</em></a></li><li>Srivastava, N., Hinton, G., Krizhevsky, A., Sutskever, I., Salakhutdinov, R. (2014). <a href="https://jmlr.org/papers/v15/srivastava14a.html"><strong>Dropout: A Simple Way to Prevent Neural Networks from Overfitting.</strong></a> <em>Journal of Machine Learning Research</em></li><li>Sun, C., Shrivastava, A., Singh, S., Gupta, A. (2017). <strong>Revisiting Unreasonable Effectiveness of Data in Deep Learning Era. </strong><a href="https://arxiv.org/abs/1707.02968"><em>arXiv:1707.02968</em></a></li><li>Deng, J., Dong, W., Socher, R., L. -J. Li, Li, Kai, Fei-Fei, Li.(2009). <a href="https://image-net.org/static_files/papers/imagenet_cvpr09.pdf"><strong>ImageNet: A large-scale hierarchical image database.</strong></a><strong> </strong><em>IEEE Conference on Computer Vision and Pattern Recognition, 2009, pp. 248–255, doi: 10.1109/CVPR.2009.5206848.</em></li><li>Ghiasi, G., Lin, T., &amp; Le, Q. V. (2018). <strong>DropBlock: A regularization method for convolutional networks.</strong> <a href="https://arxiv.org/abs/1810.12890"><em>arXiv:1810.12890</em></a></li><li>Larsson, G., Maire, M., &amp; Shakhnarovich, G. (2016). <strong>FractalNet: Ultra-Deep Neural Networks without Residuals.</strong> <a href="https://arxiv.org/abs/1605.07648"><em>arXiv:1605.07648</em></a></li><li>Wan, L., Zeiler, M., Zhang, S., Le Cun, Y. &amp;amp; Fergus, R.. (2013). <a href="https://proceedings.mlr.press/v28/wan13.html"><strong>Regularization of Neural Networks using DropConnect.</strong></a> <em>Proceedings of the 30th International Conference on Machine Learning, in Proceedings of Machine Learning Research 28(3):1058–1066</em></li></ol> <p><img src="https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=70be8d6661a7" width="1" height="1" alt=""/></p>]]></content><author><name></name></author><category term="external-posts"/><category term="medium"/></entry><entry><title type="html">The quickest way to start training on Amazon EC2 Spot</title><link href="https://kate-feingold.github.io/blog/2021/the-quickest-way-to-start-training-on-amazon-ec2-spot/" rel="alternate" type="text/html" title="The quickest way to start training on Amazon EC2 Spot"/><published>2021-03-27T17:37:45+00:00</published><updated>2021-03-27T17:37:45+00:00</updated><id>https://kate-feingold.github.io/blog/2021/the-quickest-way-to-start-training-on-amazon-ec2-spot</id><content type="html" xml:base="https://kate-feingold.github.io/blog/2021/the-quickest-way-to-start-training-on-amazon-ec2-spot/"><![CDATA[<h4>Tutorial</h4> <h4>and pay ~70% less</h4> <p>It’s simple: <em>a well-performing deep learning </em><strong><em>model</em></strong><em> </em><strong><em>=</em></strong><em> more than 1 </em><strong><em>training</em></strong><em> session for searching for good hyper-parameters of all sorts </em><strong><em>=</em></strong><em> big </em><strong><em>expenses</em></strong>.<br/>So how do you train on a budget? Ideally, the first thing on your list should be optimizing the process to <strong>avoid wasted electricity</strong>.</p> <h3>JavaScript is not available.</h3> <p>new work: nvidia-co2 displays how much arctic sea ice your GPU is melting per hour 💻🧠🧊 https://t.co/hHbNGfeMag pic.twitter.com/x6ilGVLrin</p> <p>However, in this tutorial I’d like to address the next step: that’s when you want to do<strong> the same run as usual, but cheaper</strong>.</p> <blockquote><strong>Disclaimer:</strong> It is not an extensive tutorial on EC2 and I’m going to assume you are either at least familiar with AWS Console or have been using EC2 On-Demand instances already.</blockquote> <h3>Given</h3> <p>Let’s begin with <strong>a simple basic setup</strong> in which you have:</p> <ul><li>1 instance;</li><li>1 project on it for training a network;</li><li>all the dependencies installed;</li><li>an SSH connection you are using to run a training command.</li></ul> <p>We’ll build from here and see the minimum modifications that have to be done to cut down the cost.</p> <h3>Task</h3> <blockquote>“A Spot Instance is <strong>an unused EC2 instance</strong> that is available <strong>for less than the On-Demand price</strong>. Because Spot Instances enable you to request unused EC2 instances at steep discounts, you can lower your Amazon EC2 costs significantly.” — <a href="https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/using-spot-instances.html">AWS User Guide</a></blockquote> <p>The <strong>interaction with such instances is almost the same</strong>: you will see them all in the EC2 Console and will be able to connect as usual. Now you can even temporarily stop them whenever you need, which wasn’t possible <a href="https://aws.amazon.com/about-aws/whats-new/2020/01/amazon-ec2-spot-instances-stopped-started-similar-to-on-demand-instances/">until Jan 13, 2020</a>.</p> <p>To make sure that you can benefit from this great offering we have to prepare for one caveat: <a href="https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/spot-interruptions.html"><strong>interruptions</strong></a>. Basically, whenever the demand is high and someone is willing to bid more for the resource, your instance will be interrupted. The frequency will depend on different factors such as a region, time of the day, instance type, etc. E.g. <em>p3.8xlarge</em> with 4 NVIDIA Tesla V100 GPUs might be harder to get on discount, while <em>p2.xlarge</em> with 1 NVIDIA K80 GPU should be taken rarer and will often be available again within minutes.</p> <p>The good news is that there is a way to prepare for this situation such that whenever the demand drops, <strong>your training will resume on itself</strong>. That is what will be covered in this tutorial.</p> <h3><strong>Constraints</strong></h3> <p>It is crucial to have weights, optimizer state, etc saved often and your code should <strong>support</strong> <strong>resuming training</strong> from the last checkpoint. It is generally essential for any training that takes a while, so most likely you have this feature already anyway.</p> <p>To handle interruptions you’ll need a file that will be regularly rewritten, e.g. <strong><em>checkpoint_last</em></strong>. That’s the checkpoint that will be resumed whenever necessary.</p> <h3><strong>Solution</strong></h3> <p>1 If you have an On-Demand instance running, unfortunately, you cannot turn it into a Spot one through settings straight away. Instead, <strong>get an image</strong> if you don’t have one yet.</p> <figure><img alt="EC2 Console" src="https://cdn-images-1.medium.com/max/1024/1*Amr5SIwL4QKyqw2F2dKfzQ.png"/><figcaption>Right-click on the instance -&gt; Image and templates -&gt; <strong>Create image</strong></figcaption></figure> <p>2 Create a Spot request using this AMI. Do everything as usual but during <strong>“Step 3: Configure Instance Details” </strong>check the box for “<strong>Persistent request”</strong> and select <strong>“Stop”</strong> as <strong>Interruption</strong> <strong>behavior</strong>. This combination is the first <strong>major step</strong> to continuous training and it means that if your instance is needed by someone else it’ll be stopped and started again whenever the demand drops. Don’t hit “Launch” just yet.</p> <figure><img alt="Spot instance creation" src="https://cdn-images-1.medium.com/max/1024/1*8OLJZG7DiN7qlvFhmmXOxQ.png"/><figcaption>Launch instances -&gt; Step 3: Configure Instance Details -&gt; <strong>Persistent request</strong> -&gt; <strong>Interruption</strong> <strong>behavior: Stop</strong></figcaption></figure> <blockquote><strong>Important note:</strong> If you pick <strong>“Terminate”</strong> then after interruption a new instance will be recreated from a selected AMI. <strong>“Stop”</strong> however allows you to preserve all the new files on the root volume untouched in between interruptions, which is very convenient and even <strong>required if new weights are being saved on root</strong> and not on a separate volume.</blockquote> <p>3 Let’s ensure that when the instance is restarted <strong>the</strong> <strong>training command will be run immediately</strong>. For that, you can specify the sequence of commands that will resume training in <strong>User data</strong> also in “Step 3: Configure Instance Details”. The name isn’t very self-descriptive so you might be wondering what that is. By default, it is meant to <a href="https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/user-data.html">run commands on your Linux instance at launch</a>, which doesn’t apply to restart. That’s why we have to adjust it in the following way:</p> <pre>Content-Type: multipart/mixed; boundary=&quot;//&quot;<br />MIME-Version: 1.0<br /><br />--//<br />Content-Type: text/cloud-config; charset=&quot;us-ascii&quot;<br />MIME-Version: 1.0<br />Content-Transfer-Encoding: 7bit<br />Content-Disposition: attachment; filename=&quot;cloud-config.txt&quot;<br /><br />#cloud-config<br />cloud_final_modules:<br />- [scripts-user, always]<br /><br />--//<br />Content-Type: text/x-shellscript; charset=&quot;us-ascii&quot;<br />MIME-Version: 1.0<br />Content-Transfer-Encoding: 7bit<br />Content-Disposition: attachment; filename=&quot;userdata.txt&quot;<br /><br />#!/bin/bash<br /><strong>/home/ec2-user/resume_training.sh<br /></strong>--//</pre> <p>Copied from <a href="https://aws.amazon.com/premiumsupport/knowledge-center/execute-user-data-ec2/">here</a>. Just paste it as is, we’ll create <em>resume_training.sh</em> soon. Replace <strong><em>ec2-user</em> </strong>with your Linux user name though, you cannot use<em> </em>the<em> $HOME</em> variable here.</p> <p>Keep in mind that User data can only be adjusted on instance creation and whenever it is stopped. It <strong>cannot be modified</strong> if it is <strong>in a running state</strong>. But that’s okay because the commands themselves will be enclosed in a bash script stored on the instance, so you’ll always have access to modifying it.</p> <p>Proceed with the rest of the settings as usual and launch.</p> <p>4 All the steps so far are within the basic interaction with the EC2 Console. Here comes the only adjustment you have to make: write a sequence of calls in <em>$HOME/resume_training.sh</em> that will<em> </em>resume training after your instance is restarted.</p> <blockquote><strong>Important note:</strong> Environment variables like the ones set in .bashrc won’t be accessible at the point of execution of resume_training.sh, so <strong>if you need any exports</strong> for your training, <strong>add them here</strong> as well.</blockquote> <pre># <strong>create</strong> resume_training.sh in home directory<br />cd $HOME &amp;&amp; touch resume_training.sh</pre> <pre># make it <strong>executable</strong> <br />chmod +x resume_training.sh</pre> <p>Then edit it in your favorite editor:</p> <pre># <strong>mount</strong> all the volumes used during training, e.g.<br />sudo mount /dev/xvdf /data_volume</pre> <pre># <strong>export </strong>necessary variables<br /># export...</pre> <pre># create a variable that will point to <br /># the <strong>date and time of instance restart</strong><br />d=$(date +%Y-%m-%d_%H:%M) </pre> <pre># do other preparations you need</pre> <pre># cd into your project and <strong>start training</strong>, e.g.<br />cd /path/to/your/project &amp;&amp; nohup train.py --resume /path/to/checkpoint_last.pt &amp;&gt; &#39;/path/to/log_&#39;$d&#39;.out&#39; &amp;</pre> <p>After each restart, a new training log will be created with a date indicating the moment when the instance started running again.</p> <p>5 Obtain <strong><em>checkpoint_last</em></strong>. It can be a result of the training you did earlier. For a new training session either let it run for a bit or create a dummy file that won’t break anything.</p> <p>Then simply start training as you usually do or run:</p> <pre>cd &amp;&amp; ./resume_training.sh</pre> <p>Now you can finally go do something else while training runs by itself like it is supposed to. But cheaper.</p> <h3>Comments</h3> <p>If you are still not sure about the whole pipeline, take a look at this diagram:</p> <figure><img alt="" src="https://cdn-images-1.medium.com/max/698/1*3paujMluKBC0Q2YDTCPOMg.png"/><figcaption>Created in <a href="http://draw.io">draw.io</a></figcaption></figure> <blockquote><strong>Bonus: </strong>You can receive <strong>an email notification</strong> on each interruption. To set it up follow <a href="https://ec2spotworkshops.com/running_spark_apps_with_emr_on_spot_instances/tracking_spot_interruptions.html">this tutorial</a>. It won’t influence anything in the loop above since everything takes care of itself. But if you do want to be informed about interruptions anyway, then go for it.</blockquote> <p><img src="https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=8ec58f1a181" width="1" height="1" alt=""/>&lt;hr&gt;&lt;p&gt;<a href="https://awstip.com/the-quickest-way-to-start-training-on-amazon-ec2-spot-8ec58f1a181">The quickest way to start training on Amazon EC2 Spot</a> was originally published in <a href="https://awstip.com">AWS Tip</a> on Medium, where people are continuing the conversation by highlighting and responding to this story.&lt;/p&gt;</p>]]></content><author><name></name></author><category term="external-posts"/><category term="medium"/></entry><entry><title type="html">Kick off your first research project in AI</title><link href="https://kate-feingold.github.io/blog/2021/kick-off-your-first-research-project-in-ai/" rel="alternate" type="text/html" title="Kick off your first research project in AI"/><published>2021-03-06T19:52:54+00:00</published><updated>2021-03-06T19:52:54+00:00</updated><id>https://kate-feingold.github.io/blog/2021/kick-off-your-first-research-project-in-ai</id><content type="html" xml:base="https://kate-feingold.github.io/blog/2021/kick-off-your-first-research-project-in-ai/"><![CDATA[<figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*Ilofx83UnOmE2vy834F8Kw.jpeg"/><figcaption>Photo by <a href="https://unsplash.com/@sloppyperfectionist?utm_source=unsplash&amp;utm_medium=referral&amp;utm_content=creditCopyText">Hans-Peter Gauster</a> on <a href="https://unsplash.com/?utm_source=unsplash&amp;utm_medium=referral&amp;utm_content=creditCopyText">Unsplash</a></figcaption></figure> <h4>An <strong>Introductory Guide</strong></h4> <p>Many talk about the value of research and the gems it can help uncover. The topics for learning and potential discoveries are vast and are often backed by countless publications and tutorials available online. Yet, it seems that not enough attention is given to <strong>the “how” of the process itself</strong>. Research can take many forms, have different objectives and timespans and at the end of the day may get discouraging.</p> <p><strong><em>Disclaimer</em></strong><em>: the shared links will have a strong flavor of AI, but some things should apply to other fields as well.</em></p> <h3>The process</h3> <p>For great general advice read <a href="https://web.mit.edu/tslvr/www/lessons_two_years.html"><strong>Lessons from My First Two Years of AI Research</strong></a> written by Tom Silver, PhD student at MIT, 2018.</p> <p>Here are my condensed highlights from it:</p> <ul><li>Carefully <strong>coding a baseline</strong> of the problem at hand can help discover <strong>bugs in your mental model</strong> early on.</li><li>Begin with <strong>code for visualization</strong>, it will make it easier to discover problems with your ideas. And keep in mind this:</li></ul> <blockquote>“A bad visualization will require recalling in detail the code you wrote to produce it; a good one will scream an obvious conclusion.”</blockquote> <ul><li>Use <strong>any platform that works for you</strong> to pick what to read: community (e.g. Twitter), newsletters, trusted conferences agenda, etc. Also, check out <a href="http://www.arxiv-sanity.com/"><strong>Arxiv Sanity Preserver</strong></a> by Andrej Karpathy.</li><li>Recognize <strong>the motivation of the paper</strong> and its relationship with a field of AI in general, e.g. “math”, “engineering” or “cognition”. A project that effectively combines existing techniques will lure engineers but might bore cognitive scientists and vice versa.</li><li>The <strong>best explanations</strong> are often found during <strong>invited talks</strong> by the authors because that’s where clarity (and not word count) is valued most.</li><li>Take <strong>time to reflect on a paper</strong> you’ve just read, don’t rush it.</li><li>Collect <strong>tangible evidence of accomplished work</strong> by the end of each day: an idea in detail, what was ruled out and why, conversations made, etc.</li><li>Take <strong>notes of your thinking process</strong> and of virtually anything that seems valuable at the moment, you might forget it.</li><li>When feeling stuck don’t hesitate to <strong>step back</strong>, you’ve written everything down, so you can safely come back to this later. Quote:</li></ul> <blockquote>“Backtracking is forward progress.”</blockquote> <p>For more valuable insights and structure read the original article.</p> <h3>Reading of a scientific paper</h3> <p>Now that you hopefully feel even more excited to dive into reading, watch the video tutorial by Yannic Kilcher, PhD Student at ETH Zurich, <a href="https://youtu.be/Uumd2zOOz60"><strong>How I Read a Paper: Facebook’s DETR</strong></a>. He has a lot of <strong>well-curated reviews</strong> of deep learning research, but I feel like this one is worth watching first. In this video he shares his approach and shows <strong>which sections he looks at</strong> first and which he usually skips, which require thinking critically or going over twice, etc.</p> <blockquote><strong>Small tip: </strong>Many researchers claim that a <strong>“Related Work”</strong> section is irrelevant, but I feel like sometimes it’s actually a great way to start unraveling the topic, since you can follow through the references with a bonus: authors’ assessment of them. Alternatively, look for a good <strong>survey paper</strong>.</blockquote> <p>The tool used in the video is <a href="http://www.onenote.com/"><strong>OneNote</strong></a>.</p> <h3>Organization</h3> <p>A few days into reading and papers begin to pile up. Even if you take <strong>notes</strong>, unless you organize them properly they <strong>get lost and forgotten</strong>. It’s hard to remember what was unique about each publication. Also, you might want to highlight some text inside of a paper itself. Save a tree if you can and <strong>use some software</strong> instead. There are benefits that come with it, such as an opportunity to organize your ideas into a hierarchical structure, to attach notes, collaborate with a group of people, etc.</p> <p>Let’s go over a few options out there.</p> <h4><a href="https://www.zotero.org/">Zotero</a></h4> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*Rq9pocGvGVu5yj4H4Boogg.png"/><figcaption>Web Library</figcaption></figure> <ul><li><strong>Meant for academic research</strong>: Yes</li><li><strong>Subfolders</strong>: Yes</li><li>Adding <strong>notes</strong>: Yes, both general and attached to a paper</li><li><strong>Group search</strong>: <a href="https://www.zotero.org/search/type/group">Yes</a></li><li>Group<strong> membership </strong>types: Public Open, Public Closed, Private</li><li>Supported <strong>platforms</strong>: macOS, Windows, Linux, a browser</li><li>Built-in <strong>highlights</strong> inside papers: No, only through Adobe Reader commenting tools</li><li>Chrome <strong>extension rating</strong> (Match, 2021): <a href="https://chrome.google.com/webstore/detail/zotero-connector/ekhagklcjbdpajgpjgmbionohlpdbjgc?hl=en">4</a></li><li><strong>License</strong>: Free</li><li><strong>Overall</strong> experience: My personal favorite. The desktop version looks a bit outdated, but Web Library has it all</li></ul> <h4><a href="https://www.mendeley.com/download-desktop-new/">Mendeley</a></h4> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*wz2pB4jyvOW2hmX7JyY7AA.png"/><figcaption>Library</figcaption></figure> <ul><li><strong>Meant for academic research</strong>: Yes</li><li><strong>Subfolders</strong>: Yes, but can only be created from a desktop version</li><li>Adding <strong>notes</strong>: Yes, but can only be created and viewed in a desktop version, can only be attached to a paper</li><li><strong>Group</strong> <strong>search</strong>: <a href="https://youtu.be/47NOI5HQ5m4?t=150">Yes</a>, but I wasn’t able to find it in <a href="https://www.mendeley.com/guides/web/03-group-collaboration">a new layout</a></li><li>Group<strong> membership </strong>types: Public, Invite-Only, Private</li><li>Supported <strong>platforms</strong>: macOS, Windows, Linux, a browser (fewer features), Android, iOS</li><li>Built-in <strong>highlights</strong> inside papers: Yes, with a sync feature</li><li>Chrome <strong>extension rating</strong> (Match, 2021): <a href="https://chrome.google.com/webstore/detail/mendeley-web-importer/dagcmkpagjlhakfdhnbomgmjdpkdklff?hl=en">3</a></li><li><strong>License</strong>: Free</li><li><strong>Overall</strong> experience: The desktop version looks a bit outdated, while the online Library lacks some features. Shared highlights save the day</li></ul> <h4><a href="https://paperpile.com/">Paperpile</a></h4> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*izOtEnTtu2fkflsTL4GX-g.png"/><figcaption>Paperpile</figcaption></figure> <ul><li><strong>Meant for academic research</strong>: Yes</li><li><strong>Subfolders</strong>: Yes</li><li>Adding <strong>notes</strong>: Yes, can only be attached to a paper, no formatting</li><li><strong>Group</strong> <strong>search</strong>: No</li><li>Group<strong> membership </strong>types: With an email or with a link</li><li>Supported <strong>platforms</strong>: Chrome</li><li>Built-in <strong>highlights</strong> inside papers: Yes (beta)</li><li>Chrome <strong>extension rating</strong> (Match, 2021): <a href="https://chrome.google.com/webstore/detail/paperpile-extension/bomfdkbfpdhijjbeoicnfhjbdhncfhig">5</a></li><li><strong>License</strong>: 30-day trial, then $2.99/month for academic, $9.99/month for business, billed annually</li><li><strong>Overall</strong> experience: Didn’t enjoy the design of the library, didn’t find a reason to choose it over other free alternatives</li></ul> <p><em>These are just a very few. Invest some time into finding the one that works for you, it will definitely pay off!</em></p> <h3>Implementation</h3> <p>Check out how other people solve similar problems with code, then build your own thing in a fork or in a new repository. Just don’t forget to check the license and to cite the authors if you are using someone else’s work. <br/><a href="https://paperswithcode.com/"><strong>Papers With Code</strong></a> is a great place to do that. They also recently <a href="https://medium.com/paperswithcode/papers-with-code-partners-with-arxiv-ecc362883167"><strong>partnered with arXiv</strong></a> where now you’ll find a Code tab for each publication, so see if authors left a link to Official Code.</p> <h4><a href="https://paperswithcode.com/methods">Methods</a></h4> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*2HM7n3-s6U3B8bahccBG2Q.png"/><figcaption><a href="https://paperswithcode.com/methods">https://paperswithcode.com/methods</a></figcaption></figure> <p>Visit this page regularly to keep yourself up-to-date and to check for new methods organized by areas of Machine Learning.</p> <h4><a href="https://paperswithcode.com/sota">Browse State-of-the-Art</a></h4> <figure><img alt="" src="https://cdn-images-1.medium.com/max/1024/1*hrSptjax0NvfZhDl0goInQ.png"/><figcaption><a href="https://paperswithcode.com/sota">https://paperswithcode.com/sota</a></figcaption></figure> <p>This page may introduce you to 3 very valuable notions:</p> <ul><li><strong>benchmarks</strong>;</li><li><strong>leaderboards</strong>;</li><li><strong>metrics</strong>;</li></ul> <p>You can also search for relative leaderboards on <a href="https://www.kaggle.com/"><strong>Kaggle</strong></a>. But be extra mindful about leaderboards in general. As Chip Huyen writes in her booklet <a href="https://github.com/chiphuyen/machine-learning-systems-design"><strong>Machine Learning Systems Design</strong></a> in the <a href="https://huyenchip.com/machine-learning-systems-design/research-vs-production.html#performance-requirements-5DWpHYz">Performance requirements</a> section:</p> <blockquote>“<strong>A few percentage points</strong> might be a big deal on a leaderboard, but <strong>might not even be noticeable for users</strong>.”</blockquote> <p>And also</p> <blockquote>“…a model can do <strong>better than the rest just by chance</strong> (<a href="https://lukeoakdenrayner.wordpress.com/2019/09/19/ai-competitions-dont-produce-useful-models/">AI competitions don’t produce useful models</a>, Luke Oakden-Rayner, 2019).”</blockquote> <p><em>To conclude I’d like to wish you good luck with your project. You are going to be inspired, excited, daunted, frustrated and all over again. It’s all part of the process. Just remember: </em><strong><em>discovering new things is fun</em></strong><em>! </em>🎉</p> <p><img src="https://medium.com/_/stat?event=post.clientViewed&amp;referrerSource=full_rss&amp;postId=c97caff1818f" width="1" height="1" alt=""/></p>]]></content><author><name></name></author><category term="external-posts"/><category term="medium"/></entry></feed>